# 微信小程序预览指南

## 在微信开发者工具中预览

### 方法一:直接导入已编译的代码(推荐)

1. 打开**微信开发者工具**
2. 点击 **"导入项目"** 或 **"+"** 号
3. 填写以下信息:
   - **目录**: 选择 `/Users/leolin/Desktop/blueBerry/unpackage/dist/dev/mp-weixin`
   - **AppID**: `wxb19ad7426dfb8bd4` (已在 project.config.json 中配置)
   - **项目名称**: blueBerry
4. 点击 **"导入"** 即可预览

### 方法二:使用开发模式(实时编译)

1. 在项目根目录运行:

   ```bash
   npm run dev:mp-weixin
   ```

2. 等待编译完成后,在微信开发者工具中导入:
   - **目录**: `/Users/leolin/Desktop/blueBerry/unpackage/dist/dev/mp-weixin`

3. 开启**自动预览**功能,代码修改后会自动重新编译

## 项目结构说明

```
blueBerry/
├── pages/              # 页面源码
├── static/             # 静态资源
├── utils/              # 工具函数
├── unpackage/          # 编译输出目录
│   └── dist/dev/mp-weixin/  # 微信小程序编译结果 ← 在微信开发者工具中打开此目录
├── App.uvue            # 应用入口
├── main.uts            # 主脚本
├── pages.json          # 页面配置
├── manifest.json       # 应用配置
└── package.json        # 依赖配置
```

## 注意事项

- ✅ 编译输出目录: `unpackage/dist/dev/mp-weixin`
- ✅ AppID: `wxb19ad7426dfb8bd4`
- ✅ 开发模式下会自动监听文件变化并重新编译
- ⚠️ 正式发布请使用 `npm run build:mp-weixin` 进行生产环境编译

## 常用命令

```bash
# 开发模式(实时编译)
npm run dev:mp-weixin

# 生产环境编译
npm run build:mp-weixin
```
