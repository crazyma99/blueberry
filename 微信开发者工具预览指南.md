# 微信小程序预览指南

## 源码与产物

- 源码根目录: `src/`
- 静态资源目录: `src/static/`
- 开发产物: `dist/dev/mp-weixin`
- 生产产物: `dist/build/mp-weixin`

根目录不维护 `pages/`、`static/`、`utils/`、`App.uvue`、`main.uts`、`pages.json`、`manifest.json`、`uni.scss` 副本。

## 开发预览

1. 在项目根目录运行:

   ```bash
   npm run dev:mp-weixin
   ```

2. 编译完成后,在微信开发者工具中导入:

   ```text
   /Users/leolin/Desktop/blueBerry/dist/dev/mp-weixin
   ```

## 生产构建预览

1. 在项目根目录运行:

   ```bash
   npm run build:mp-weixin
   ```

2. 编译完成后,在微信开发者工具中导入:

   ```text
   /Users/leolin/Desktop/blueBerry/dist/build/mp-weixin
   ```

## 项目结构

```text
blueBerry/
├── src/
│   ├── pages/
│   ├── static/
│   ├── utils/
│   ├── App.uvue
│   ├── main.uts
│   ├── pages.json
│   ├── manifest.json
│   └── uni.scss
├── package.json
├── vite.config.ts
└── project.config.json
```
