# 微信真机自验清单（Phase 3 收口卡口 · 2026-09-17 立）

> 用途：把「真机证据」这唯一剩余卡口变成**可照着做、可回填**的清单。您或测试同学按表执行，把结论（✅/❌＋备注）发我，我据此勾 `P3-11`／`P3-15`／`P0-11`／`P2-15`／`P2-16` 并**正式收口 G3**。
> 构建产物：`cd miniapp-vue3 && npx uni build -p mp-weixin` → 用微信开发者工具导入 `dist/build/mp-weixin`（或直接「预览」出二维码真机跑）。

## 0. 环境与前置
| 项 | 值 |
|---|---|
| 后端基址 | `generated/profile.config.ts → apiBases`（`trial` 域；如需 prod 请告知） |
| 登录 | 首页/我的页首次进入会**静默换票**（`uni.login → POST /api/wx/login`）；「我的」页可再做手机号登录补齐资料 |
| 需准备 | ①一张**合格人像照**（短边≥480、不模糊、单人、人脸占比 2%–65%）②可用的**推荐次数/试衣次数**（或按 4001 走充值）③微信支付测试环境（若要验买断） |

## 1. `P3-11` 保存与买断（结果页 `aiTryOnResult`）
| # | 操作步骤 | 预期结果 | 记录 |
|---|---|---|---|
| 1.1 | 从试衣历史进任一 **completed** 结果页 | 页面显示结果图；**预览上有 24 片水印**，无报错 | ☐ 截图 |
| 1.2 | 直接点「保存到相册」 | 首次弹**相册授权**；同意后保存成功，toast「已保存到相册」；**保存的原图不含水印** | ☐ 截图／成片 |
| 1.3 | 拒绝授权后再点保存 | 弹「需要您授权保存图片到相册／去设置」；**不扣费**；去设置开启后再点可保存 | ☐ 备注 |
| 1.4 | 付费模式且余额 0 时点保存 | 走共享支付面板；**取消**→静默回到页面（不扣费、不保存） | ☐ 备注 |
| 1.5 | 支付成功 | 到账后**只自动保存一次**（不重复保存，账户只扣一次） | ☐ 余额变化截图 |
| 1.6 | 已买断任务再点保存 | 角标显示「已解锁 · 永久保存」；**不再扣费** | ☐ 截图 |
| 1.7 | 匿名分享落地（他人分享的作品页） | 只读：**无付费下载入口**；仅「我也要试」 | ☐ 截图 |

## 2. `P3-15` 试衣纵切（`aiTryOn` → `aiTryOnResult` → `aiTryOnHistory`）
| # | 操作 | 预期 |
|---|---|---|
| 2.1 | 试衣页：左右滑切换模板、点缩略图直达、折叠/展开缩略条 | 联动正确、选中态金描边 |
| 2.2 | 选体型（瘦/微胖/胖）、年龄 | 选择生效（AppSegment/AppSelector 无异常） |
| 2.3 | 上传照片：选一张**模糊**或**短边<480**图 | 被拦截并给出对应文案（**前端拦截**；换合格图可继续） |
| 2.4 | 上传合格图 → 点「生成效果」 | 提交成功 → 进结果页 → 等待态进度条推进 → 完成后展示结果 |
| 2.5 | 生成中**立刻返回**再进结果页 | 按任务状态恢复轮询（不重复创建任务、不重复扣费） |
| 2.6 | 进「AI试衣记录」→ 从结果页返回 | 列表**静默刷新**（不整页骨架闪） |
| 2.7 | 试衣页/结果页**切后台再回**、以及**截屏** | 截屏时页面内容隐藏（防截屏生效）；回前台行为正常 |
| 2.8 | 无次数时点生成 | 4001 → 拉起充值；**取消**不扣费；充值成功后自动续跑一次（**不重复扣**） |

## 3. `P3-13` 分享三分支
| # | 操作 | 预期 |
|---|---|---|
| 3.1 | 结果页「分享」给好友 → 好友点开 | 正常打开作品页（可看结果） |
| 3.2 | 「朋友圈」分享 → 自己从单页模式打开 | 单页模式：**不跳页**，引导前往小程序；作品**只拉一次、不轮询** |
| 3.3 | 分享卡片封面 | 显示网络 JPG 封面；断网/空 URL 有兜底（不显示破图） |

## 4. T9b 推荐（同步扣费纪律）
| # | 操作 | 预期 |
|---|---|---|
| 4.1 | 推荐入口：上传照片 → 开始分析 | 进等待页；**只发 1 次请求**（不得重复 POST 当轮询） |
| 4.2 | 分析中等待 | 伪进度推进；**超时 180s 只判失败**、不自动再扣 |
| 4.3 | 无推荐次数 | 4001 → 充值；取消不扣；成功后续跑**一次** |
| 4.4 | 结果页 | 每条建议显示「N分」（仅 `finalScore>0` 时显示分数）；「查看模板」跳转正确 |

## 5. 首页下拉刷新（2026-09-21 新增：主人「首页下拉刷新功能丢失了」）
| # | 操作 | 预期 |
|---|---|---|
| 5.1 | 首页顶部**下拉**（松手触发） | 出现系统指示；页面内容**不回落骨架屏**（已有内容保留） |
| 5.2 | 松手后观察刷新反馈 | 顶部浮出**品牌金 spinner＋「刷新中…」**胶囊（wot `wd-loading`）；刷新完成自动消失 |
| 5.3 | 刷新是否真的取到新数据 | 改一次 OPS 轮播/店铺配置后下拉，**banner 与店铺卡片随新配置变化**（banner 每次带防缓存 `t`，旧端口径） |
| 5.4 | ⭐ 指示器位置 | 自绘胶囊**不与系统原生下拉指示（微信三点）重叠**；若重叠请截图（位置常数 `NATIVE_PULL_BAND_PX=48` 可调，见 deviations #27） |
| 5.5 | 弱网（飞行模式/限速）下拉 | 指示器在收口后**必消失**（不卡住）；失败时页面保留旧内容并可重试 |
| 5.6 | 抖音端（若走 `tma preview`） | 胶囊与文案可见；**spinner 是否旋转/环是否成形/文案字号**＝本轮 `unknown`，按实际结果回填（deviations #27 已登记降级风险） |

## 6. 下拉刷新扩围与分享补迁（2026-09-21 主人四项点名）
| # | 操作 | 预期 |
|---|---|---|
| 6.1 | 客片列表页（`demoDetail`）下拉 | 重新拉列表（分类态整页重载；搜索态重跑搜索）；金胶囊指示器出现→消失；数据为最新 |
| 6.2 | 客片详情页（`targetPhotoDetail`）下拉 | 重载详情＋点赞态；指示器正常收口 |
| 6.3 | AI试衣记录页（`aiTryOnHistory`，**仅微信**）下拉 | 重载任务列表＋页脚文案；指示器正常 |
| 6.4 | 价目表 tab（`priceHomePage`）下拉 | 重载门店网格＋页脚/联系信息；指示器正常 |
| 6.5 | ⭐ 位置检查（5 页） | 自绘金胶囊**不与原生三点重叠**、不压自绘标题栏；胶囊内距观感（本轮已加大到 16rpx/24rpx） |
| 6.6 | 首页转发给好友 / 分享到朋友圈 | 卡片标题＝OPS `share_card` 配置（无配置则代码默认）；首页好友卡片落地带 `?brandId=` |
| 6.7 | 客片列表页 / 客片详情页转发 | 卡片标题/封面按配置；落地路径＝`?idx=`（列表）/`?idx=&type=`（详情）；无 `idx` 时落首页 |
| 6.8 | 切品牌后分享 | 标题里 `{brand}` 显示为当前品牌名（非占位符、非空串） |
| 6.9 | AI试衣结果页转发（好友） | **卡片封面以人脸为中心（5:4）**＝VK 生效；若走中心裁剪请记录（模拟器不支持 VK 属正常） |
| 6.10 | AI试衣结果页「分享到朋友圈」 | 仍为网络 JPG 缩略图（bug #11 口径不变），卡片能显示 |

## 7. `P0-11` 基准采集（需同设备同网络）
| 指标 | 采集方式 | 记录 |
|---|---|---|
| 首页冷启动 → 首屏可见 | 秒表/录屏帧计 3 次 | ☐ 三次耗时（取 p50） |
| 相册首图加载 | 同上 | ☐ |
| 试衣任务返回（提交 → 结果页可见） | 同上 | ☐ |
| 推荐结果返回 | 同上 | ☐ |

> 规则：**只记实测值，不填估计值**；敏感图片/账号请脱敏。

## 8. 回填方式
把上表结论（✅/❌＋截图或备注）发我，我负责：①据此更新 `phases.md`（`P3-11`／`P3-15`／`P0-11`／`P2-15`／`P2-16`）②更新 `g3-acceptance.md` 结论 ③有 ❌ 就开修复批次（开发→单测→CR→三平台）。
**任何一项 ❌ 都不隐瞒，直接进修复流程**——台账以证据为准。
