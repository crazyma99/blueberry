# 待主人拍板事项（一页决策单）— 2026-09-17

> 现状：**Phase 0–3 的功能/测试/CR 已全部做完**，剩余未勾项**全部依赖下列外部输入**；Phase 4 门禁＝G3 通过。
> 每项都给「选项＋我的建议＋不做会怎样」，您只需回一句话（例：「1 走 A、2 同意、3 待定、4 选 A」）。

## D1. 微信真机验收（阻塞 **G3 收口** 与 **Phase 4 开工**）
- **怎么做**：照 `device-acceptance-checklist.md` 走一遍（§1 保存/买断 7 步、§2 试衣纵切 8 步、§3 分享三分支、§4 推荐扣费 4 步、§5 基准采集），把结论发我。
- **建议**：您或测试同学跑（我**无法**代跑真机；本机无微信开发者工具 CLI）。
- **不做会怎样**：`P3-11`／`P3-15`／`P0-11`／`P2-15/16` 长期挂账，**G3 不能收口**，Phase 4（抖音/小红书适配）**按门禁不得开工**。
- 备选：若您希望**不待真机先开工 Phase 4**，请明确授权「接受 G3 暂缺真机证据」（我会把该授权与风险写进 phases）。

## D2. `P0-12`／`P4-01` 跨平台账号口径（阻塞 Phase 4 后端合同）
- **问题**：微信/抖音/小红书的 **账号、手机号、余额、买断** 能否共用？绑定/解绑规则？
- 选项：**A 保持 provider 隔离（推荐）**｜B 允许共用（需后端出映射合同 `平台+appid+subject→internalUserId`）。
- **建议**：**A**——母方案即「未确认时保现有合同」，且跨端共用涉及资金与合规，不建议在无合同前放开。
- **不做会怎样**：Phase 4 的 `P4-01/02/03` 无法开工（后端合同无输入）。

## D3. 平台 AppID ＋ 后台合法域名（阻塞抖音真机出码）
- **需要**：各平台 AppID（抖音现为 `testAppId` 占位）＋ 管理台 request/upload/download 域名、H5 CORS、COS 权限（`P4-06`）。
- **建议**：至少先给**抖音 AppID**（构建侧我已用合成 Profile 打通 E2E，真机出码只差 AppID）。
- **不做会怎样**：抖音真机 `tma preview --qrcode-output` 无法执行；`P1-06/25/28` 的工具侧证据只能停在 `tma project-size`（已取得）。

## D4. 旧仓（生产仓）`sync-template.sh` 护栏（`P1-33`）
- **现状**：旧仓该脚本用 `rsync -a --delete` 同步 `src/pages`、`src/utils`，**无「禁止指向新端」护栏**；若误指到新端会 `--delete` 掉迁移成果。
- 选项：**A 我在旧仓加一行护栏（推荐）**——target 含 `miniapp-vue3` 或目标缺 `App.uvue` 时 `exit 1`（**需您明确授权我改生产仓**）｜B 维持操作纪律，不加护栏。
- **不做会怎样**：`P1-33` 按「部分满足」长期挂账；风险靠人工纪律承担。
