# 已知偏差登记（P0-09；行为修正必须写明产品依据与测试，不盲保缺陷）

1. **package/lock 历史差异**：以 preflight §0 实测哈希为准（lock 60a176ad…／pkg 9f522349…）。
2. **finalScore 类型缺失**：旧端 AI 推荐结果 `finalScore` 运行时有值、类型未声明 ⇒ 新端补齐类型（嘉仪 PR#1 已对齐后端降序口径）。
3. **下载 paid 与权益到账竞态**：旧端 payGuard.uts 统一支付门闩＋到账双条件 paid&&balance>0＋续跑前重拉余额＋不自动二次拉起支付 ⇒ 新端保语义。
4. **品牌馆开关刷新口径（plan §7 有意修正，2026-09-17 校正表述）**：新端**冷启动（首页 onMounted）**刷新开关，**同上下文在飞请求去重**（brand-hub-controller 按 scopeRevision 去重、切品牌作废在飞）；旧端 60s 内存缓存＋下拉/热恢复强制刷新**不再保留**（controller 无缓存，且当前**未实现** onPullDownRefresh——原表述「下拉/热恢复强制刷新」与代码不符，此处更正；**2026-09-21 更新：首页已实现 onPullDownRefresh，见 #27**）。**2026-09-17 P2-20 追加**：品牌馆页切品牌后回首页，由**首页 onShow 检测品牌基线变化**触发 bumpScope＋复位入口开关＋重载（旧端 index:218-232 同序，非偏差），保证「入口显隐」与「品牌数据」同步，杜绝入口显示而进入被拦。
5. **vitest 版本适配（2026-09-17 实测）**：vitest 5.0.1 需 vite `./module-runner` 导出（vite 6+），与冻结的 vite 5.2.8 不兼容（启动即 ERR_PACKAGE_PATH_NOT_EXPORTED）⇒ **降级锁 vitest 3.2.4**（其 dependencies.vite＝^5.0.0），不为测试框架盲升 vite。
6. **工具链测试自身两处 bug（2026-09-17 首跑抓出并修复）**：①精确版判定正则写错（`[^~]` 语义相反）②`@dcloudio/types@^3.4.8` 属独立版本线被误判跨线 ⇒ 修正为显式首字符判定＋发行线检查限定 `3.0.0-` 族；**负向 fixture 先红后绿**，10/10 通过。
7. **品牌馆页解析口径（P2-20 CR 🟡4，已知低风险）**：gate 取件的严格性由 client `isBusinessSuccess`（0/200 均成功）承担，旧端 `pageConfig.uts:11` 为严格 `code===200` ⇒ 若接口返回 `code=0` 旧端隐藏、新端可能放行（实测返回 200，概率低）。`domain/brand-hub.parseBrandHubResponse` 保留为旧端口径的规格测试锚点，生产链路已不经它。
8. **webview URL 准入加固（P2-22，**有意修正**）**：旧端 `pages/webview/index.uvue:24-26` 对 `url` 参数**零校验**（`decodeURIComponent` 后直接透传 `<web-view :src>`）⇒ 可打开任意外部 URL（含 `javascript:`/`data:` 等）。**产品依据**：phases P2-22「webview 只允许既有合法 URL 策略…不放开任意 URL」。**新端口径**：仅 `http(s)` 且 host 精确命中或其子域（白名单＝Profile `apiBases` 派生域，兜底 `lanmei66.cloud`/`crazyma99.xyz`；沿用 `imageLoader.uts:33-38` 精确主域口径防子串伪装）；非法/缺失 → 不渲染 web-view＋toast「链接不可打开」＋800ms 返回。协议（policies/user、policies/privacy）与联系信息（ServiceContact 电话/二维码）走**站内页**，不经 webview；第三方 URL 一律拒绝。**测试**：`tests/unit/t29-webview.spec.ts`（6 例：白名单主域/子域/大小写/端口/userinfo 放行；外域/伪装子串/`javascript:`/`data:`/`file:`/相对路径/空值拒绝；页面拒绝路径＋返回按钮）。

9. **推荐空载荷判失败＝有意比旧端更严（2026-09-17，T9b CR 🟡3）**：旧端判据为 `(code===0||200) && res.data`（`aiRecommendLoading:146`），在 JS 真值语义下 `{}` 与 `{analysis:null,recommendations:[]}` **都算成功**（转场到**空白结果页**）；新端 `application/ai-recommend-flow.ts` 对「整包全空」判 `BUSINESS` 失败并给「推荐结果为空，请重试」。**无误伤**：真正合法的「有 `analysis`＋空 `recommendations`」仍 `ok:true`。**产品理由**：空白结果页对用户是死路，失败提示＋重试入口更可用；如需逐字等价请指示。
10. **finalScore 数字字符串口径（2026-09-17，T9b CR 🟡5，可选登记）**：旧端 `v-if="rec.finalScore > 0"` 在 `"90"`（字符串）时成立会显示「90分」；新端 `normalizeFinalScore` 仅接受 `number` ⇒ 字符串不显示。现行后端 DTO 为 number，**实务等价**。

11. **字体全局类未定义（2026-09-17 补登，CR 🔴2 触发）**：旧端 `App.uvue` 全局类 `font-noto-serif`／`harmony`（宋体/鸿蒙字族）在新端**未定义**，样式沿用既有口径：字号按旧值**字面量**还原（如旧 `--font-size-body=24rpx` ≠ 新 `tokens.semantic.fontSizeBody=32rpx`，不得混映射），字体族差异由后续主题批次统一处理。**产品依据**：字体族缺失属视觉细节，不阻断功能闭环；**测试**：各批页面单测按字面量断言字号（如 `t22`／`t26`／`t32`），本表 `platform-capability-matrix.md` 引用本条。此条为补齐「矩阵引用不存在的登记」的缺口而补登（此前该偏差**未登记**，违反「偏差必须进 deviations」纪律）。

12. **店铺封面追加 `lazy-load`（2026-09-17，性能偏差；排查「模拟器卡死」时引入）**：旧端 `components/PhotoGrid/PhotoGrid.uvue` 的店铺封面（`<image mode="aspectFill">`）**无懒加载**（`grep lazy-load` = 0），新端初版按忠实移植同样未加；但首页会**一次性渲染全部店铺封面**，在微信模拟器/低端机上首屏容易长卡。**改法**：仅给该 `<image>` 追加 `lazy-load`——**只改变加载时机**，不改布局类名、尺寸、圆角与点击行为。**产品依据**：性能与可感知卡顿；**测试**：`tests/components/ui-contract.spec.ts`、`t6-index.spec.ts` 与三平台构建均通过（无断言依赖图片加载时机）。

13. **骨架栅格用「包裹 view + class」替代通配选择器（2026-09-17，平台强制；首次体验版上传实测触发）**：`pages/index/index.vue` 与
    `pages/priceHomePage/index.vue` 的骨架占位原用 `.sk-grid > * { flex: 1 }` / `.sk-row > * { flex: 1 }`（**通配选择器 `*`**）。
    新版代码在 `uni build` 阶段不报错，但**微信 wxss 编译器拒绝 `*`**：体验版上传时编译失败
    `wxss 编译错误 ErrorFileCount[2] ... error at token \`*\``（`pages/index/index.wxss`、`pages/priceHomePage/index.wxss`）。
    **改法**：子元素各包一层 `<view class="sk-cell">`，CSS 改 `.sk-cell { flex: 1 }`——**视觉与布局等价**（`.sk-grid/.sk-row` 仍为
    `display:flex; gap:8rpx`，两个占位仍各占 1 份），仅多一层 view（骨架为临时占位，不影响业务 DOM）。
    **产品依据**：微信 wxss 不支持通配选择器（平台限制，非可选）；**测试**：全量 vitest 400 passed、`vue-tsc` 0 错、三平台构建 0，
    且**产物实测无 `*` 选择器**（`grep '*.data-v'` 与通配正则均 0 命中），体验版上传成功（`v1.0.25`）。

14. **「暗→亮」偏差撤回，深色主题还原（2026-09-19，主人拍板）**：T4 起 tokens semantic 层曾按亮色定义（`colorPage=#FFFFFF`、正文 #333/#666 等），各页注释称「全仓迁移主题口径」，但**主人从未拍板**；抖音端实测「页面背景应为黑色却是白色」后撤回。**还原口径**：`tokens/source.json` semantic 改回旧端深色实测值——`colorPage→primitive.ink #160F04`、`colorTextPrimary/Strong→gold #F1CD91`（旧端正文/标题主色，`demoDetail.uvue:997/:1132`、`index.uvue:758`）、`colorTextSecondary→rgba(241,205,145,0.7)`、`colorTextMuted→rgba(241,205,145,0.5)`、`colorBorder/Divider→rgba(241,205,145,0.3)`；`pages.json` globalStyle 底色/导航栏同步深色（对齐旧端 `src/pages.json:67-72`）。为此 `generate-tokens.mjs` 字面值校验放开 `rgb()/rgba()`（标准 CSS 颜色函数，wxss/ttss 均支持），并补 primitive 层进 theme.scss/theme.css。页面级亮色适配残留（黑文字/黑底骨架在深底不可见）已按旧端 uvue 逐值还原（favorites/mine/brandHub/aiTryOnHistory/demoDetail，详见各文件 2026-09-19 注释）。policies 两页旧端本就是白底黑字，保持不动。**已知遗留**：`mine` 页 `.profile-group` 金色系描边为接入期自主偏差（`84a24f0`），非本次亮色残留，未动。

15. **抖音 TTSS 不支持 CSS 变量 ⇒ 全仓消除运行时 CSS 变量（2026-09-19，平台强制）**：抖音官方 TTSS 文档「变量等特性编译暂不支持，依赖宿主内核兼容性」。Vue3 SFC `v-bind("tokens…")` 编译产物为 `var(--hash)`＋内联注入（`useCssVars`），抖音端整条丢弃 ⇒ 体验版「分类 tab 样式全丢/页面白底」根因。**改法**：①184 处 `v-bind("tokens.*")` → scss 编译期 `$变量`（`scripts/codemod-vbind-scss.mjs`，style 块补 `lang="scss"`；`vite.config.ts` 以 `additionalData` 注入 `src/generated/theme.scss` 原文，token 单源不变）；②直写 `var(--*)` → 字面量（`scripts/codemod-css-var-literal.mjs`，映射表取自 App.vue `page{--*}` 定义块/design-token.md，注释跳过不篡改）；`App.vue` 的 `page{--*}` 定义块随之删除；③`GenerationProgress` 局部 `--gp-node-size` 改 scss 局部变量。**验证**：mp-toutiao 产物 `grep var(--`/`useCssVars` 清零（唯一残留为 wot-ui 探针页引用的 uni 框架变量 `--window-bottom`，非生产路径）。**产品依据**：平台限制，非可选；微信/H5 视觉不变（值为编译期同一常量）。

16. **抖音端自定义导航栏对齐微信（2026-09-19，主人拍板）**：原口径「抖音 navStyle=default、CustomNavBar 渲染空、系统栏接管」导致抖音端所有页面无自绘导航，且 slot 内容（客片列表/收藏的搜索框）一并消失。现撤回：pages.json 九个业务页 `navigationStyle: custom` 去除 `#ifdef MP-WEIXIN` 全平台生效（AI 六页本就仅微信注册，不受影响），CustomNavBar 删除 `nativeHidden` 平台门控全平台渲染；同步补回 demoDetail/favorites 搜索框 `placeholder-style`（金色 70% 字面量，不用 var()）。**测试**：`t6-components.spec.ts` 抖音用例反转为「与微信同口径渲染」。**风险**：抖音右上胶囊（含「反馈」位）比微信宽，搜索栏等 slot 内容在窄屏可能与其视觉相碰，待真机目视。

17. **抖音导航口径二次回退＋全端页脚距底 20px（2026-09-19 晚，主人实测反馈）**：#16 的「抖音 navigationStyle custom 全平台生效」实测**未生效**——抖音平台 custom 导航需在开放平台「功能管理-页面结构自定义」**申请权限**（未申请则平台仍绘制默认栏：logo＋页面标题＋胶囊，且配置 custom 会阻碍代码包上传），叠加 CustomNavBar 全平台自绘 ⇒ 我的/价目表等页**双标题＋双顶部占位**（IDE 模拟器截屏实证）。**回退口径**：①`pages.json` 九个业务页恢复 `#ifdef MP-WEIXIN` 包裹 `navigationStyle: custom`（抖音回到 default 系统栏，单标题）；②CustomNavBar 抖音端仅**内联渲染 slot**（favorites 搜索框/targetPhotoDetail 标题组——#16 要救的 slot 内容保留），自绘标题栏＋占位仅微信渲染；demoDetail 搜索框本批已下移页面首行，不受影响。③页脚：主人指示「底部文案距底给 20px」——PageFooter 四变体下边距 32rpx→40rpx（=20px），抖音端文案不再贴原生底 tab，微信 tab 页/子页同步抬高。**测试**：`t6-components.spec.ts` 抖音用例再反转为「不自绘标题栏、仅透传 slot」。**后续可选**：若主人在抖音开放平台申请「页面结构自定义」权限通过，可重新评估 #16 方案（届时仍需处理抖音 custom 下系统 logo/胶囊不可隐藏）。

18. **demoDetail 搜索框回导航栏 slot＋AI 试衣按钮/详情底部补齐（2026-09-19，主人实测反馈「渲染混乱」逐条对齐原版微信）**：①demoDetail 搜索框从页面首行**回到 CustomNavBar slot**（旧端 :4-19/:929-958 逐值还原，与 favorites 同款：描边胶囊壳＋内置搜索 icon、无独立搜索按钮、键盘 confirm 触发、manual-back 有值先清空）；在 #17 口径下该 slot 微信＝自绘栏内、抖音＝系统栏下内联首行，两端同源（#17 注「demoDetail 搜索框下移页面首行」由此条取代）。②demoDetail 列表卡**补回 AI 试衣按钮**（旧端 :164-167/:1091-1110：desc-main 右侧 140rpx 占位＋desc-tryon 绝对定位垂直居中，`/static/aitry-btn.png` 随之拷贝进新端 static）；AI 页仅微信注册 ⇒ 按钮 `platform==='mp-weixin'` 门控（跳转函数同口径守卫）。③targetPhotoDetail **补回 BottomActionBar**（旧端 :77-90：AI 试衣按钮组 slot——微信门控；内置版权 footer 全平台——即主人所见「底部描述文案」；页脚文案经 page-config 用例注入，口径同 demoDetail/favorites）＋200rpx 底部占位（旧 :605-608）；`goToAiTryOn` 五参数口径（albumId/shopId/category/subCategory/style，hapticTap＋style 重编码）。**测试**：全量 vitest 414 passed；mp-weixin/mp-toutiao 双构建产物实证——微信 demoDetail.json 含 `navigationStyle:custom`、抖音无（#17①生效）；两端 wxml/ttml 均见 search-bar-wrap/desc-tryon/ai-tryon-btn，BottomActionBar 组件产物含 bottom-bar/bar-copyright，`aitry-btn.png` 入两端 static 并经 common/assets.js 绑定引用。

19. **搜索框盛满导航栏剩余区域＋AI 推荐横幅几何还原（2026-09-19，主人实测「搜索框偏小/AI 模块底部截断」）**：①demoDetail/favorites 导航 slot 搜索框 `width:65%`→`flex:1`（margin 0 20rpx）——旧端 65% 建立在旧 CustomNavBar slot **无胶囊避让**的满宽上（≈430rpx）；新端 slot 已被胶囊避让 paddingRight≈95px 收窄，照抄 65% 只剩 ≈306rpx。slot 避让层保证填满后不撞胶囊。②demoDetail AI 推荐横幅按旧端 :1191-1226 还原：banner 定高 **178rpx**（初版背景图在流撑 160rpx＋内容 absolute 超高 ⇒ 被 overflow 裁底）、`margin:24rpx 20rpx`、背景图绝对铺满、内容相对定位 height:100% **双向居中**（初版 absolute inset:0＋左对齐 padding 系偏差）、标题 38rpx（--font-size-display，初版误取 sub-title 档）/副标题 22rpx·margin-top 16rpx·letter-spacing 1rpx（初版 8rpx/caption 档）。**测试**：全量 vitest 417 绿（t24 抖音风味文案失败系后台连跑 mp-toutiao 构建后 `src/generated/profile.config.ts` 残留抖音 profile 所致——`gen-profile-local.mjs mp-weixin` 回切微信快照即绿，非本次改动回归）；双端构建产物实证 `.search-bar-wrap{flex:1}` 与 banner 178rpx 写入 wxss/ttss。

20. **PhotoGrid 容器补 `box-sizing:border-box`（2026-09-19，抖音价目表「店铺列表贴紧右侧」实测）**：`.demoPhotoContaner` 为 `width:100%`＋`padding:0 8rpx`——旧端 uvue 默认 border-box 不溢出；vue3 mp 端默认 content-box ⇒ 右溢 16rpx，右列店铺卡贴紧/溢出右屏边（微信同机制，8rpx≈4px 不易察觉）。卡片级 border-box 此前已补（本组件 :80 注释），容器级本次补齐。**页脚 20px 口径核查**：PageFooter 四变体下边距 40rpx（=20px）经 #17 已全部生效，本次双端产物复核 wxss/ttss 逐变体在案（beian/bottomdesc/copyright/bottomdesc-dark 及 BottomActionBar 的 28rpx+12rpx=40rpx）；mine 页新旧端均无版权 footer，不涉及。**测试**：全量 vitest 绿＋双端构建产物实证 `.demoPhotoContaner` 含 `box-sizing:border-box`。

21. **PageFooter 分隔线以文案为闸（2026-09-19，主人实测「客片列表 loading 下方有一条横线」）**：loading 期页脚无条件渲染，`.divide`（2rpx 15% 白线）先于文案出现，双端可见。**口径**：底部文案不展示之前不出横线——divide 改为 `withDivide && (mainLine/supportLine 任一行非空)` 才渲染（loadFooter 兜底链 override→OPS→Profile→默认保证就绪后非空）。组件级收口，9 页同源生效，无需逐页改。**测试**：`page-footer.spec.ts` 增闸门用例（空串不渲染→文案到达后出现），copyright/wrapped 两例补文案 props 维持原断言；全量 vitest 绿＋双端产物实证 divide 为 `wx:if/tt:if` 条件节点。

22. **客片详情「服务价格/套餐内容」两行恢复无条件渲染（2026-09-19，主人实测「抖音客片详情没展示套餐内容」）**：初版迁移给两行加了 `v-if` 空值守卫（`price != null`、`packageDesc != null && !== ''`）——**未登记的自主偏差**；旧端 :46-54 无条件渲染，空值时仍显示「套餐内容：」标签行。实测 trial 接口（`GET /wechat/album/detail`，平台无关、无 platform 头）对空套餐相册返回 `packageDesc:""`（如相册 463），新端整行消失 ⇒ 主人所见「没展示」。**还原**：两行去掉守卫与旧端一致（空值渲染空串，标签保留）。**测试**：全量 vitest 415 passed＋双端构建产物实证 `.package` 行无 `wx:if/tt:if` 守卫。

23. **PageFooter beian 下间距 margin→padding（2026-09-19，主人实测「抖音首页/价目表底部描述仍贴底 tab」）**：#17/#20 已把 `.beian` 下边距抬至 40rpx 且双端产物在案，但抖音端**页面滚动高度不计最后一个元素的 margin-bottom**（小程序通坑，margin 在 CSS 里但不产生视觉间距）⇒ 40rpx 形同虚设。**改法**：`.beian` 下间距 `margin: 40rpx auto` → `margin: 40rpx auto 0; padding-bottom: 40rpx`（padding 计入盒高、必计入页面高度）；仅 beian 变体（index/priceHomePage 两 tab 页，主人点名范围），bottomdesc/copyright/bottomdesc-dark 为子页页脚无底 tab，未动。微信端视觉不变（`.beian` 无背景，padding 与 margin 等价；其后 tabbar-safe-spacer 衔接不变）。**测试**：`page-footer.spec.ts` 7 绿＋全量 vitest 415 passed＋抖音产物 ttss 实证 `.beian{margin:40rpx auto 0;padding-bottom:40rpx}`。

24. **`X-Brand-Id` 恢复旧端「有品牌上下文即全请求恒带」（2026-09-19，主人拍板）**：初版 client 设计了 `brandScoped` 开关（「仅品牌作用域才带」），但**无任何仓储标记** ⇒ 新端实际从不携带品牌头；旧端 `http.uts:122` 为全请求口径（扫品牌码/品牌馆切牌后所有业务请求都带，空串＝无品牌上下文走后端旧逻辑）。排查「vue3 首页无品牌馆入口」时发现：该现象本身是**体验域 OPS `brand_hub.enabled=false`**（正式域 lanmei 为 true，两端解析口径一致，非代码 bug）；但品牌头缺口会让「品牌馆切牌/扫品牌码」场景在正式域拿错品牌数据（如 huahua 无 `brand_hub` 配置应隐藏入口）。**改法**：`client.ts` 头注入改为 `brandId 非空即恒带`，删除 `brandScoped` 字段；上传通道 `platform/uni/upload.ts` 本已注入不受影响。**测试**：`http-client.spec.ts` 两例反转为新口径（有上下文恒带／null 与空串均不带）；vue-tsc 0 错＋全量 415 passed＋双端构建通过。HANDOFF §3 头注入口径同步更正。

25. **登录链路平台分流抽象（2026-09-19，主人拍板「先把前端这层平台分抽象好」，D2「抖音登录后端未开发」的前端前置）**：原 `uni.login({provider:'weixin'})` 与 `/api/wx/login|/api/wx/phone` 两端点全平台硬编码。**改法**：①`platform/uni/login.ts` 增 `LOGIN_PROVIDERS` 查表（微信 weixin／抖音 toutiao／mp-xhs 省略 provider），`createUniLoginCode({ platform })`；②`infrastructure/repositories/wx-auth.ts` 增 `AUTH_ENDPOINTS` 查表（**服务端契约位·唯一事实源**：微信 `/api/wx/*` 线上定案；抖音 `/api/tt/login`、`/api/tt/phone` 为**占位契约**，待服务端仓库按 SOP 定案后核对；未登记平台如 mp-xhs 不发请求直接 fail-closed `{kind:'UNKNOWN',retryable:false}`），`createWxAuthRepository({ client, platform })` platform 改必填；③14 个页面装配与 `application/login-flow.ts` 同步传 platform（逐页已核 platform 先于调用点定义）。**行为变化**：抖音端静默换票由「打 /api/wx/login 必失败」改为「打 /api/tt/login 404」——同属 fail-closed 无会话，UX 等价不劣化；服务端抖音端点落地后抖音登录零前端改动即可用。LoginPopup 手机号按钮（抖音 button 支持 getPhoneNumber 但需开放平台申请「手机号快速验证」能力）与头像昵称（chooseAvatar 微信专属）留待服务端契约定案后随登录批次处理。**测试**：`repositories.spec.ts` 增平台分流用例（微信定案路径／抖音占位路径／mp-xhs 零请求 fail-closed）、`t43-silent-login.spec.ts` 增 provider 分流用例（toutiao/weixin/省略）；全量 vitest 417 passed、`vue-tsc` 0 错、双端构建产物实证 TT 包 `process.env.UNI_PLATFORM` 折叠为 `"mp-toutiao"`、`AUTH_ENDPOINTS` 含 `"mp-toutiao":{login:"/api/tt/login",bindPhone:"/api/tt/phone"}`。

26. **客片详情加载态还原骨架屏（2026-09-19，主人实测「新版客片详情加载为什么不是骨架屏」）**：初版用 `LoadingBlock` 文案占位——**未登记的自主偏差**；旧端 :13-35 为骨架屏（容器复用真实类 1:1 镜像）且 :232-237「详情+点赞就绪后预加载前 3 张详情图（首图原图/其余 750 缩略、2500ms 超时）再收起骨架」。**还原**：①模板按旧端逐值重建骨架（标题 52%×46rpx／云朵位 122×40rpx／价格 58%×32／套餐 76%×32／收藏圆 56rpx／图 500rpx×2 间距 8rpx），容器类用新端对应真实类（`.meta`＝旧 `.title`、`.title-first/.info-row/.info-left/.photo-wrap`），外层 `.sk-container`（App.vue 全局类）＋inline `padding:0`（同旧端覆盖）；②`init` 就绪前补 `preloadImages(urls, 2500)`（URL 走 `progressivePhotoSrc` 与渲染同口径）；③`platform/uni/image-preload.ts` 补 `preloadImages`（旧端 imageLoader.uts:22-27：Promise.all race 超时、单张失败不打断）；④LoadingBlock 引用与 `.sk-wrap` 样式随删。**测试**：`t19-detail.spec.ts` 断言类名 `.sk-wrap`→`.sk-container`；全量 vitest 417 passed、`vue-tsc` 0 错、双端构建产物实证骨架节点与几何 props（52%/122rpx/500rpx×2/56rpx）在案。

14. **「我的」页 AI 试衣菜单项：渐变字 → 品牌金（2026-09-20，平台强制）**：旧端 `src/pages/mine/index.uvue:603-609` 的 `.special-font`
    用 `linear-gradient(...)` ＋ `-webkit-background-clip:text` ＋ `-webkit-text-fill-color:transparent` 做**渐变字**；新端逐字移植后，
    **微信小程序渲染器不支持 `background-clip:text`**（产物 wxss 保留该声明但真机不生效）⇒ 文字退化为渐变首个色 `#ffb26f`（**橙金**），
    与同列「我的喜欢」（品牌金 `$color-action`）不一致，主人 2026-09-20 反馈要求修复 ⇒ 改为 `color: $color-action`（统一品牌金）。
    **影响面**：仅该菜单项文字颜色；**测试**：vitest 全绿、三平台构建 exit 0。

27. **首页下拉刷新恢复（2026-09-21，主人：「小程序的首页下拉刷新功能丢失了，下拉刷新使用 wotui+design token 完成」）**：
    **事实**：旧端 `src/pages/index/index.uvue:252-263` 有 `onPullDownRefresh`、旧端 `src/pages.json:5-8` 首页 `enablePullDownRefresh: true` ＋ `backgroundTextStyle: "light"`；
    新端 T6 首页接入期二者**均未迁**（本表 #4 曾如实登记「当前未实现 onPullDownRefresh」）⇒ 主人真机下拉无反应，判定「功能丢失」。
    **恢复口径（对齐旧端，不新造交互）**：①`pages.json` 首页开 `enablePullDownRefresh: true` ＋ `backgroundTextStyle: "light"`（仅首页，逐字段对齐旧端）；
    ②`index.vue` 注册 `onPullDownRefresh` ⇒ `refreshHome()`：并行重载轮播（**带防缓存 `t`**，旧端 :356 口径）／店铺／品牌馆开关／页脚联系内容 → `brandHubOn` 同步
    → `updateCurrentBanner()` → **`fgTick+1` 重挂载前景层重播渐入动效**（旧端 :258）→ `uni.stopPullDownRefresh()`；**成功与意外异常都收指示器**（旧端 .then/.catch 双分支
    ⇒ 新端 `finally`，异常另留 `console.error("[index] …")` 痕）；**刷新期不回落骨架屏**（旧端同：已有内容保留）；连拉守卫（在飞不重入）。
    ③**指示器＝wot `wd-loading`**（主人指定 https://wot-ui.cn/component/loading.html）——**经本项目门面 `src/ui/BaseLoading.vue` 消费**（业务视图零 `wd-*`，plan §26/§229；独立 CR 🔴1 整改项，页面与产物 `usingComponents` 均只见门面）；
    门面默认值＝design token：spinner 色 `tokens.semantic.colorAction`（#F1CD91）、尺寸新增**组件级 token** `component.pullRefreshLoadingSizeRpx=48rpx`
    （`tokens/source.json` 唯一手改 → 重生成 tokens.ts/theme.scss/theme.css，digest `2210080…`→`3a2cfc6…`）；页面侧胶囊底 `$color-page`／描边 `$color-border`／内距 `$space-xs $space-sm`
    （编译期字面量，**页面自身规则零 `var(--`、零通配 `*`**，守 #13/#15，产物 grep 实证）。
    ⚠️ **抖音端降级风险（如实登记，独立 CR 🔴2 更正）**：`color` 经组件 `rootStyle` 落根节点**内联** `style.color`（非 var）⇒ **颜色两端有保障**；但 wot 组件**自身样式是 `var()` 链**
    （产物 `mp-toutiao/.../wd-loading.ttss` 实测：`animation: wd-rotate var(--wot-loading-spinner-animation-duration,.8s)`、`mask: radial-gradient(… var(--wot-loading-spinner-circle-stroke-width,15%) …)`、
    文案 `font-size: var(--wot-loading-text-font-size, …, 14px)`）⇒ 按 #15 机制（抖音 TTSS 不保证变量）**抖音端「spinner 是否旋转／环是否成形／文案字号」为 `unknown`，待 `tma` 真机**；
    已同步进 `platform-capability-matrix.md`（下拉刷新行：微信 supported（产物级）／抖音 unknown）。**不得据本条宣称抖音已支持**。
    **有意差异（登记）**：旧端下拉反馈＝**平台原生**指示（微信三点）；新端在此之上**叠加品牌自绘指示器**（主人本轮明确要求），原生指示并存。
    位置随平台：`top = (isMpWeixin ? statusBarHeight : 0) + 48px`——48px ＝ 原生下拉指示带约 40px ＋ token `spaceSm`（16rpx＝8px）间距；微信端首页沉浸式无导航栏（页面原点＝屏幕顶部）故叠加状态栏；
    抖音端为系统栏页面（**页面坐标原点已在系统栏之下**：`CustomNavBar` 抖音分支只内联 slot、不占位）故**不叠加**（独立 CR 🟡2 更正：初版按微信口径叠加 52px 属双计状态栏）。⚠️ **微信端「自绘胶囊 vs 原生三点」是否重叠＝待真机目视**（已进 `device-acceptance-checklist.md`）。
    **未纳入本轮**：客片页（demoDetail）旧端同样有下拉刷新（旧端 `pages.json:16-22`），主人本轮只点名首页 ⇒ 保持未开，待指示。
    **与旧端的差异（据实修正，独立 CR 🟡4）**：①旧端成功分支含 `loadShareCard()`，而**新端首页整体没有分享卡片**（无 `onShareAppMessage`/`onShareTimeline`）＝**既有未迁项**（本轮不含，已同步登记 `parity.md` 首页行）；
    ②`refreshHome` 额外刷 `loadStaticContent()`（页脚/联系信息 2 次 `/api/page-config`）＝**超出旧端、有意**（旧端这两块无 props 自源；新端口径＝「下拉即取最新 OPS 配置」，代价＝弱网下指示器多停留）；
    ③旧端 `reloadHomeData` 开头重置 `heroHeightReady/heroHeight`（随首图重算），新端 `heroHeight` 为固定 794rpx 且无 `@load` 重算（自适应属既有未迁项）⇒ **该重置在新端无对应物、无新缺口**（页面注释原引 P2-13 不准，已改）。
    **平台事实（uni-app 官方《页面配置》表逐行核对，2026-09-21）**：`enablePullDownRefresh` 无平台限制说明（微信／抖音同口径生效）；`backgroundTextStyle` **平台差异列＝仅微信小程序** ⇒ 抖音端该字段被忽略（**原生下拉指示点用抖音默认色**，本轮品牌指示器＝我们的 `wd-loading` 胶囊，为其主要视觉）；微信端下拉露出的窗口底色由 `globalStyle.backgroundColor: "#160F04"`（玄墨）保证**不白闪**（`backgroundColor` 官方说明＝「下拉显示出来的窗口的背景色」，仅微信／小红书）。
    **测试**：`tests/unit/t49-index-pull-refresh.spec.ts`（9 例：配置仅首页开启／两个刷新键在 `#ifdef` 块**之外**／页面零 `wd-*`／页面样式零 `var(--`＋零通配／回调注册＋重新取件＋防缓存 `t`／
    门面 token 色尺寸文案／**前景层重挂载 `fgTick` 重播**／连拉守卫／意外异常仍收口并留痕）＋`tests/components/ui-contract.spec.ts` 门面 `BaseLoading` 2 例（token 默认值＋全量透传）＋`t6-vms.spec.ts` 防缓存用例；
    全量 vitest **430 passed／3 skipped**、`vue-tsc` **0 错**、三平台构建 **exit 0**，产物实证：微信/抖音 page json 含 `enablePullDownRefresh:true`＋`backgroundTextStyle:"light"`
    （微信另含 `navigationStyle:custom`）、wxml/ttml 含 `.refresh-indicator` 且 `usingComponents` 只登记**门面**组件、wxss/ttss 页面规则为 token 字面量
    （`background:#160f04`／`rgba(241,205,145,.3)` 描边／`padding:8rpx 16rpx`）且**零 `var(--`、零通配 `*`**。
    **变异测试（独立 CR 🟡3 方法，本轮自测六组）**：删 `enablePullDownRefresh`／删防缓存 `t`／删 `finally` 里 `stopPullDownRefresh()`／删 `fgTick+1`／门面默认色改死值／页面改回直用 `<wd-loading>`
    ⇒ **六组均按预期变红**（分别 2／2／4／1／2／2 例失败），复原后 39 例全绿 ⇒ 上述断言非「只写类名」的弱断言。
    ⓘ **顺带发现（未改，待主人拍板）**：`profiles/blueberry/project.env:11` 的 `BRAND_NAME` 仍为「蓝梅旅拍」，与 `c0e9bb0` 已改名的生成快照
    `src/generated/profile.{config.ts,json}`（「蓝梅云」）不一致 ⇒ **每次跑 `gen-profile-local.mjs` 都会把生成快照刷回旧品牌名**；本轮实测该字段**全仓无消费方**（`PROFILE.brandName` 零引用、产物内文案「蓝梅旅拍」零命中）⇒ 无用户可见影响，故本轮未动品牌源文件（品牌文案属主人拍板项）。

28. **4 页补下拉刷新＋分享链路补迁（2026-09-21，主人四项点名）**：主人原话「①自绘金胶囊的内 padding 加大一些 ②客片列表页、详情页、AI试衣列表页、价目表 tab 都要加下拉刷新 ③首页/客片详情分享卡片迁移遗漏了 ④AI试衣结果分享的 VK 人脸算法裁切也遗漏了，顺便检查所有的分享相关逻辑有没有迁移遗漏！」
    ①**胶囊内 padding**：`components/PullRefreshIndicator` 内距 `$space-xs $space-sm`（8rpx 16rpx）→ **`$space-sm $space-md`（16rpx 24rpx）**（项目自身样式仍零 `var(--`、零通配符；产物实证 `padding:16rpx 24rpx`）。
    ②**4 页下拉刷新**：旧端只有「首页＋客片列表页」开 `enablePullDownRefresh`（旧端 `src/pages.json:5-8`／`:16-22`）⇒ 客片列表页属**补回旧端口径**（旧端 `demoDetail.uvue:348-355`＝搜索态重跑搜索、否则整页 `initPage`），客片详情／AI试衣记录／价目表三页＝**本轮有意新增**（旧端无）。实现＝共享内核 `composables/use-pull-refresh.ts`（注册 `onPullDownRefresh`／连拉守卫／成功与异常都 `finally` 收口 `uni.stopPullDownRefresh`／异常留 `[label]` 痕）＋共享指示器 `components/PullRefreshIndicator.vue`（首页同步收敛到同一份实现，替换原页内联胶囊）。指示器位置口径：`(微信端 ? statusBarHeight : 0) + max(原生下拉指示带 40px, 自绘标题栏 44px) + token spaceSm 8px` ⇒ 沉浸式首页 68px、自绘标题栏页面 72px、抖音端 48px（页面坐标原点已在系统栏之下，见 #27／#17）。
    ⚠️ **抖音端范围**：AI 六页（含 `aiTryOnHistory`）仅微信注册（既有口径）⇒ 该页下拉刷新**只在微信侧存在**；`demoDetail`／`priceHomePage` 抖音端同开（产物实证 `pages/*/index.json` 含两键、无 `navigationStyle`）。
    ③**分享卡片补迁（3 页）**：旧端 `utils/share.uts` 三层兜底（品牌 `share_card` 槽位 > 中台全局同键 > 代码默认；字段级兜底；`{brand}` 占位符＋品牌名 60s 缓存）＋ 旧端三处调用点（`index.uvue:265-278` 含朋友圈、`demoDetail.uvue:292-298`、`targetPhotoDetail.uvue:164-170`）**新端此前整体未迁**（`parity.md` 首页行仅标 `ported`、未提分享卡片）⇒ 本轮新建 `application/share-card.ts` 忠实移植，三页接线 `onShareAppMessage`/`onShareTimeline`（title/path/imageUrl 与旧端逐字：首页带 `?brandId=`、客片列表 `idx` 有/无两分支、客片详情 `?idx=&type=`），并在「首屏／下拉刷新／切品牌」三处加载（旧端同覆盖）。
    **默认分享图不入包（2026-09-21 主人订正：「分享图的来源 url 来自于后台配置」）**：`share_card` 槽位下发的 `imageUrl`（COS URL）才是图片真源，
    代码默认卡片**不再内嵌** `share-home/album/aitryon.jpg`（那 3 张共 487 KB，把主包从 1.61 MB 顶到 **2113 KB ⇒ 体验版上传被平台拒**
    `80051 source size 2113KB exceed max limit 2MB`）；`imageUrl` 兜底留空时微信按默认行为用「当前页面截图」作卡片图。
    **与旧端的唯一行为差**：旧端「OPS 未配置分享图」时展示内嵌默认图，新端改为微信默认截图（若 OPS 已配置——线上实际情形——两端行为一致）。
    尝试留痕：先压缩到 600×480（317 KB→207 KB）仍偏紧，最终按主人口径**整份移出包体**（`src/static/share-*.jpg` 已不在新端）。
    **据实登记的差异**：⑴新端 `page-config` 仓储无缓存（单一事实源＝brand-hub-controller 口径，deviations #4）⇒ 分享卡片解析每次一次 GET（调用点仅三处、频次低）；旧端另有 60s 缓存。⑵成功判定用 `res.ok`（client 兼容 0/200）代替旧端 `code === 200`（同 aiTryOnResult 偏差 ⑥）。⑶`aiTryOnResult.prepareShareCard` 的回退图沿用新端 bug #11 口径 `cosThumbJpg(400)`（旧端好友卡片回退为 `cosThumb` webp、朋友圈才是 JPG）——**有意保留**（JPG 对卡片兼容性更稳）。
    ④**VK 人脸居中封面补迁**：旧端 `utils/faceShareCard.uts`（1:1 移植旧端链路：`getImageInfo` → 离屏 2D canvas 降采样 ≤1024 → VK `detectLargestFace` → 5:4 裁剪窗〔750×600，人脸中心对齐＋clamp，噪声阈值 2%〕→ 输出离屏 canvas → `canvasToTempFilePath`；VK 超时 10s）⇒ 新端新建 `platform/weixin/face-share-card.ts`（复用既有 `platform/weixin/vk-face.ts`；`vk-face.ts` 类型补 `origin`〔旧端裁剪窗依赖它，类型此前缺〕），并接回 `aiTryOnResult.prepareShareCard`（原偏差清单 ②「只走网络 JPG」的**一行替换点**）：VK 卡片优先、不可用/未检出/异常 → 回退 `cosThumbJpg(400)`。**朋友圈路径不变**（仍网络 JPG，bug #11 口径）。
    ⑤**全量分享盘点（两端逐一对照，结论＝4 处遗漏、其余已在册）**：旧端点位＝`utils/share.uts`／`vkFace.uts`／`faceShareCard.uts`／`photoCheck.uts`（用 VK）／三页 `onShareAppMessage`（＋首页 `onShareTimeline`）／`aiTryOnResult` 的 `shareToken`／`shareReadOnly`／`showShareMenu`／`buildShareQuery`／`getSharedAiTryOnResult`（`/api/aiface/tasks/share/{token}`）／`aiTryOn` 的 `share_from` 落地。**本轮补的 4 处＝①②（下拉）外，③④（卡片与 VK 封面）**；其余**全部已在册**（`application/ai-share-routing.ts`、`platform/weixin/vk-face.ts`＋`photo-check.ts`、`repositories/ai.ts` 的 `getSharedTask`、`aiTryOnResult` 的 `openShareMenu`/`shareReadOnly`、`aiTryOn` 的 `share_from` 落地，`aiRecommend` 旧端本就无分享逻辑）。
    **测试**：新增 `t50-pull-refresh.spec.ts`（10 例：内核注册/位置三态/连拉守卫/finally 收口/异常留痕/无 API 守卫＋共享指示器〔含 padding 断言〕＋4 页 pages.json 与接线静态守卫）、`t51-share-card.spec.ts`（7 例：三层兜底/字段级兜底/占位符与无品牌分支/60s 缓存与失败降级＋三页分享对象逐字段）、`t52-face-share-card.spec.ts`（9 例：裁剪窗四态＋无 wx→null＋VK 端到端裁剪参数＋VK 不可用中心裁剪＋噪声阈值＋取图失败降级），并向 `t18/t19/t22/t30` 各补 1 例「下拉 ⇒ 重数据＋收口一次」；全量 **vitest 460 passed／3 skipped**（CR 修正后 463／3）、`vue-tsc` **0 错**、三平台构建 **exit 0**；**变异测试六组**（去 `{brand}` 替换／反转 demoDetail `idx` 分支／去人脸噪声阈值／去 statusBar 项／胶囊 padding 回退／价目表页关 `enablePullDownRefresh`）**六组均按预期变红**（3／1／1／2／1／1 例），复原后 26/26 绿。
    产物实证：微信 5 页 `pages/*/index.json` 均含 `enablePullDownRefresh:true`＋`backgroundTextStyle:"light"`（微信另含 `navigationStyle:"custom"`）；抖音 `demoDetail`/`priceHomePage` 同含两键且无 `navigationStyle`（`aiTryOnHistory` 不注册于抖音）；5 页 wxml 含 `.pull-refresh-indicator`；胶囊规则编译为 token 字面量且组件产物零 `var(--`；三张分享图入 `static/` 并被 `application/share-card.js` 引用；`platform/weixin/face-share-card.js`＋`vk-face.js` 入产物；页面 JS 见 `createPullRefresh({label:…})` 调用。

    **⚠️ 独立 CR 修正留痕（同一笔内收口，CR 报告 1🔴＋11🟡）**：
    **🔴1（真机必触发，已修）** `platform/weixin/face-share-card.ts` 检测段 `drawImage` 误写 **9 参** ⇒ 微信 `drawImage` 的 2–5 参才是「源图矩形」语义（[官方](https://developers.weixin.qq.com/miniprogram/dev/api/canvas/CanvasContext.drawImage.html)）⇒ 送进 VK 的是**原图左上角 dw×dh 裁切区**而非整图降采样；触发条件 `max(w,h) > 1024`，而两端预览图都是 `cosThumb(url,1080)` ⇒ **真机必命中**，多数情形检不到脸⇒静默退回中心裁剪（＝主人点名的「VK 人脸裁切」实际不生效）。已改回 **5 参**（与旧端 `faceShareCard.uts:108`、本仓 `photo-check.ts:105` 同口径），并补 `t52` 断言锁「检测段＝整图缩放」（参数个数 4）⇒ 变异 M5（改回 9 参）**现能变红**。
    **🟡 已采纳**：②**首页补 `onLoad`**（`?brandId=`／小程序码 `scene` 写入品牌上下文；此前首页全文件无 `onLoad` ⇒ 分享卡片里带的 brandId **无人消费＝空转**，首访用户拿到全局卡片/数据；顺序按 mp 侧 `setup→onLoad→onMounted→onShow` 处理，只写上下文＋同步品牌基线，不重复 `init`）＋`t51` 增用例；③**去切品牌时的重复解析**（`init()` 内已含 `loadShareCard`，删多余一次 `GET /api/page-config`）；④`t50` 增「页面级位置接线」断言（4 页 `hasCustomNav: true`／首页默认）——原变异「删 demoDetail 旗标」曾 15/15 全绿；⑤`t49` 弱断言 `toContain("BaseLoading")`（当时只由注释满足）改为 `PullRefreshIndicator`；⑥`t18`/`t19` 注释据实更正（失败来源＝本文件 `repositories/albums` 桩 `ok:false`，同形于真实 client 失败；真 client＋空 transport 的失败路径由 `t6-index`／`t21` 覆盖）；⑦**客片列表页补旧端 `onReachBottomDistance: 80`**（旧端 `src/pages.json:17-22` 有、新端全仓漏迁，本轮同一 style 块补齐）；⑧`parity.md` 首页行改为「已补分享卡片＋onLoad 落地」并去掉本轮误加的重复行；⑨**提交范围**排除 `src/generated/profile.{config.ts,json}`（生成脚本按 `profiles/blueberry/project.env` 回刷 `brandName`，属无关回退）与 `._.DS_Store`；⑩**客片详情页下拉连页脚一起刷**（口径与首页/价目表/AI记录页对齐），并把 `init` 内 `preloadImages(urls,2500)` 的注释据实更正为「`Promise.race` 超时上限 ⇒ 弱网首次可能多停 ≤2.5s」；⑪内核注释注明 `hasCustomNav` 在抖音端的语义差异（抖音无自绘栏，44px 实为避开内联 slot ⇒ 是否重叠待真机 checklist §6.5）。
    **CR 复核的测试口径更新**：全量 **vitest 463 passed／3 skipped**（较 460 增 3 例：🔴1 检测段锁 + 🟡4 位置接线 + 🟡2 onLoad 落地）＋`vue-tsc` 0；变异现为 **7 组**（去 `{brand}`／反转 idx 分支／去噪声阈值／去 statusBar 项／padding 回退／去 finally 收口／**检测段改 9 参**）全部变红，复原后全绿。
    **待真机**（已进 `device-acceptance-checklist.md` §6）：4 页下拉指示器与原生三点是否重叠；抖音端两页下拉表现；VK 人脸封面真机效果（开发者工具模拟器不支持 VisionKit ⇒ 走中心裁剪，与旧端同）；分享卡片标题/封面在真机是否按 OPS 配置呈现。

29. **分享准备 loading 改用「wot popup ＋ wot loading」公共组件（2026-09-21，主人指示）**：主人先要求「AI 试衣落地页分享的准备 loading 也使用 wotui 的 loading 组件＋token」，
    随后追问「这个 loading 是否可以使用 wot-ui 的 popup 与 loading 去做替代，做成公共组件，后面可以复用」⇒ 落地＝**新增门面层公共组件 `src/ui/BaseLoadingPopup.vue`**：
    · **组合**（事实源＝`npx wot info Popup`／`npx wot info Loading`，`@wot-ui/cli` 1.1.0 实测）：`wd-popup`（`position=center`、`closable=false`、
      **`close-on-click-modal=false`**＝加载中不许点遮罩关、`root-portal`）＋ 门面 `BaseLoading`（内部 `wd-loading`，token 默认色 `semantic.colorAction` 金／
      尺寸 `component.pullRefreshLoadingSizeRpx` 48rpx）；卡片品牌面走 token（`$color-page` 玄墨底／`$color-border` 金 30% 描边／`$popup-radius-rpx` 圆角／`$space-lg` 内距）。
    · **白面消除**：`wd-popup` 默认表面色 `--wot-popup-bg` 为白（产物实证 `var(--wot-popup-bg, …, white)`）⇒ 组件用 `custom-style="--wot-popup-bg: transparent"`
      把 wot 面置透明、品牌面由自有 token 卡片承担（**不使用** `:deep()` 打洞 wot 内部样式；`custom-style` 经 wd-transition 落在同带 `.wd-popup` 的节点，独立 CR 已双向验证成立）。
      **注**：同串里的 `--wot-popup-radius: 0` 实为**空转**（`.wd-popup--center.is-round` 只在 `round=true` 时命中，本组件未传 `round`）——独立 CR 🟡4 据实更正，保留该声明仅为防御性。
    · **层级（真机可见项，独立 CR 🔴1）**：wot `wd-popup` 默认 `z-index: 10`，而本仓自绘标题栏 `z-index: 998`（不透明）⇒ 默认值下**遮罩盖不住顶栏**（不压暗、返回键可点），
      被删的旧页内遮罩原为 `z-index: 999` ⇒ 属回归。组件已**显式传 `z-index`（prop 默认 1001）**，与门面家法一致（BasePopup 1000／BasePicker 1000／BaseDialog 1100／下拉指示器 1000）。
    · **平台分支**（同 `BasePopup`／`BaseDialog` 家法，migration §8.12 方案 A）：非抖音端走 `wd-popup` 链（`root-portal` 生效）；抖音端自绘 fixed 蒙层＋居中 token 卡片
      （绕开 wot 宿主节点 fixed 失效；该端也不吃 `var(--wot-*)` ⇒ 透明面那段 var 只在微信链出现，符合 deviations #15 边界）。
    · **接入**：`aiTryOnResult` 分享准备遮罩由「页内自绘 `.share-preparing-mask`＋`.share-preparing-card`＋本地 `LoadingBlock`」收敛为一行
      `<BaseLoadingPopup :show="sharePreparing" text="正在准备分享…" direction="vertical" />`（`direction` 竖排＝旧端 `LoadingBlock` 口径；页内遮罩/卡片样式随之删除）。
      **可复用**：后续上传/提交等等待态可直接复用本组件（`show`＋`text` 两个入参即可）。
    · **与旧端的关系（据实，独立 CR 🟡4）**：旧端该遮罩为页内自绘（`:1320-1347` 遮罩＋卡片＋`.share-preparing-text`）⇒ 本项属**有意升级**（换 Wot 组件与 token 化），
      **几何/视觉逐项变化（非「语义不变」）**：卡片内距 `48rpx 64rpx`→`32rpx`（`$space-lg`）／圆角 `14rpx`→`24rpx`（`$popup-radius-rpx`）／底色 `rgba(22,15,4,.92)`→`#160F04` 不透明＋新增 `2rpx` 金 30% 描边／
      spinner `80rpx` 环→wot circular `48rpx`（token）／文案 `32rpx`·金 70%→`28rpx`（wot 默认 14px）·全金；全屏遮罩与「居中小卡＋竖排 spinner＋文案」的结构不变；
      **兜底**：分享准备整链新增 **12s 总超时**（`prepareShareCard`，> VK 10s 超时）——遮罩不可点遮罩关/无关闭按钮，独立 CR 🟡9 指出「极端回调不触发会永久停留」⇒ 加 race 收口；
      旧类 `.share-preparing-text` 仍按「忠实优先」保留在页样式中（页面已不渲染它）。
    · **测试**：`tests/components/ui-contract.spec.ts` 增 `BaseLoadingPopup` 3 例（微信分支 props／`show=false` 受控／抖音自绘分支）；`tests/unit/t53-share-preparing-loading.spec.ts`
      页面级 4 例（未点分享不显示 → 准备中弹层＋token 指示器 → 就绪收口用准备结果 → 降级收口回退网络 JPG → 复用/门面纪律静态守卫）；全量 **vitest 470 passed／3 skipped**、`vue-tsc` 0、
      **`npx wot lint src`＝No lint issues（45 文件）**、**`npx wot usage src` 实证 `wd-*` 仅出现在 `src/ui/` 门面（＋探针样页）**。
    · **ⓘ 工具入口（主人 2026-09-21 指示：「以后涉及 wotui 的部分都可以通过 wot skill 和 wot cli 来获取帮助」）**：本仓 `@wot-ui/cli@1.1.0` 已装 ⇒
      `npx wot info <组件>`（props/events/slots/CSS 变量）／`doc`／`demo`／`token`（组件变量↔token 名）／`lint`／`usage`／`doctor` 为**权威事实源**；
      `npx wot agent status|init|list` 可接入 MCP/Skill（支持 claude／cursor／vscode／codex／opencode／antigravity；**本仓当前未安装**，实测 detected＝Claude Code／VS Code／Codex／OpenCode）。

30. **AI 试衣记录页网格与卡片对齐「相册列表」标准（2026-09-21，主人指示）**：主人原话「AI试衣列表的 Grid 与相册列表 Grid 样式不一致，要求保持统一（以相册列表 Grid 为标准）；且卡片样式也要以相册列表的卡片为标准做样式对齐」。
    **性质＝新的统一决策（非旧端还原，据实留痕）**：旧端两页本来就不同——旧端 `pages/aiTryOnHistory/index.uvue:236-292` 为 `.photolistContainer/.photoItem`（宽 `362rpx`／高 `482rpx`／圆角 `8rpx`／白字 `28rpx`／`margin: 0 8rpx 8rpx 0`），旧端 `pages/demoDetail/index.uvue:1051-1133` 为 `.photoItem-wrap`（描边外框）＋`.photoItem`（内圈 `460rpx`/`13rpx`）＋金标题 `26rpx`；新端相册列表＝后者（类名 `album-*`，2026-09-19 已登记为「旧端结构还原」）⇒ 本轮把 AI 记录页**整块换成相册标准的类名与逐值样式**。
    **对齐内容**：网格 `.album-grid`（`padding: 24rpx 24rpx 0`／`gap: 16rpx`）／卡框 `.album-card`（`width: calc((100% - 16rpx)/2)`＋`box-sizing: border-box`＋`1rpx solid rgba(241,205,145,.3)`＋`radius: 14rpx`）／内圈 `.album-inner`（`460rpx`＋`radius: 13rpx`＋`overflow: hidden`）／封面／蒙层（同款三段渐变）／描述区（`left/right/bottom: 20rpx` 列向）／标题（`26rpx` 品牌金＋单行省略＋`font-noto-serif`）；**点击面由整卡改为蒙层**（与相册列表一致）；骨架改 `sk-row sk-grid`＋6 灰格且 `.sk-wrap` 同值 `32rpx`；AI 专属「生成中／生成失败」状态层**保留**（语义与动画不变），几何随新卡（`100%`／`13rpx` 同心）、配色归品牌金系（描边 `rgba(241,205,145,.3)`／文字与圆点 `#F1CD91`）。
    **同族第三页（我的收藏）**：其框架六条与标准**逐值相同**（仅类名为旧名 `.photolistContainer/.photoItem-wrap/.photoItem/.photo/.mask/.desc`）⇒ 本轮只补 `.photoName` 的**单行省略三件套**（原缺 ⇒ 长标题溢出/换行）；其 `.content` 本就是 `padding: 0 0 32rpx`（无横向扣减）。
    **独立 CR 两条 🔴（均已修，各一行级）**：**R1 真机必坏**——`.status-overlay`（`z-index:2`、100%×100%）是点击面 `.album-mask` 的**兄弟且在上层**，小程序命中测试取最上层元素 ⇒ 「生成中/生成失败」卡**真机点不动**（旧端点击在卡片根，故可点）；VTU 直接 `trigger(".album-mask")` 属**假绿**（`t30` 已按此更正并改由 t54 断图案）⇒ 修法＝状态层 `pointer-events: none`（本仓先例 `brandHub:199`／`mine:467`）让手势穿透到蒙层。**R2 未达要求**——AI 页 `.content{padding:0 8rpx 32rpx}` 与网格 `24rpx` 叠加 ⇒ 有效左右内边距 **32rpx**（标准 24rpx）、卡窄 8rpx（旧端那 8rpx 是配 `362rpx×2` 老数学的，换 `calc` 公式后属重复扣减）⇒ 修法＝`.content` 改 `padding: 0 0 32rpx`（与「我的收藏」同值）。
    **🟡 同期采纳**：骨架容器对齐标准（`.sk-wrap` `16rpx 0`→`32rpx`）；**守卫加固**（见下）；提交面剔除 `src/generated/profile.{config.ts,json}`（生成脚本回刷 `brandName`，非本批语义）与 `._.DS_Store`；`aiTryOnHistory` 头注「骨架 4 卡」等过期表述据实更正。
    **漂移守卫（把主人的「保持统一」变成可执行断言）**：`tests/unit/t54-media-grid-parity.spec.ts` **9 例**——①标准页自身等于**逐串钉死值**（`EXPECT` 表）②对齐页同样等于钉死值 ③共享选择器在各页**恰好出现一次**（防「追加同名规则盖掉上一条」）④`.sk-wrap`／`.content` 容器口径 ⑤旧类名不得回流 ⑥状态层保留＋**不吞点击** ⑦次级行同族（`.album-time` ≡ 相册 `.like-row`/`.like-count` 的 10rpx/24rpx/金）⑧双页骨架同口径 ⑨收藏页框架六条同值＋标题省略三件套。
    **验证**：全量 `vitest` **480 passed／3 skipped**（新增 t54 九例；`t30` 断言同步到 `.album-card/.album-cover/.album-title/.album-time`，点击面改 `.album-mask`）、`vue-tsc` **0 错**、三平台构建 **exit 0**；**变异测试 7 组全部变红**——①AI 改共享值 ②**标准页**改值（双向生效）③旧类名回流 ④追加同名规则 ⑤**三页同改** `gap`（治「一起改错」）⑥删状态层 `pointer-events`（R1 回归）⑦改 `.content` 横向 padding（R2 回归）。
    **产物级证据**：微信 `pages/demoDetail/index.wxss` 与 `pages/aiTryOnHistory/index.wxss` 的 7 条共享选择器声明**逐字相同**（`node`/`python` 双脚本比对）；收藏页 `.photoName` 编译为 `font-size:26rpx;font-weight:400;color:#f1cd91;white-space:nowrap;overflow:hidden;text-overflow:ellipsis`。**抖音端不适用**：AI 六页仅微信注册（产物无该页）⇒ content-box（#20）等抖音坑不涉及；相册列表页两端同源。
    **待真机**：`device-acceptance-checklist.md` **§7**（六条：两页卡片一致／标题金+衬线+单行省略／长标题不溢出／状态卡可点进结果页／骨架同款／抖音端说明）。

31. **抖音端底 tab 图标尺寸优化（2026-09-22，主人指示；微信侧不动）**：主人原话「抖音侧底 tab 是不是和微信不是一套代码，抖音侧仅能使用抖音默认底 tab 导航，目前抖音侧底 tab 的 icon 太大，针对抖音侧进行 icon 大小优化（微信侧不动）」。
    **①两套代码（实测确认）**：**微信＝自绘 `custom-tab-bar`**（`src/custom-tab-bar/{index.js,wxml,wxss,json}`；图标 `/static/iconpark/{home,price,mine}[-filled].svg`，渲染 **38rpx×38rpx**；页面侧 `tabBar.custom: true`）；**抖音＝`pages.json` 原生 `tabBar`**（`custom` 缺省；图标 `static/{home,price,my}-bar[-dim].png` 6 张，由平台渲染）。⇒ **6 张 PNG 仅抖音使用**（微信自绘栏不引用；`t55` 以此为断言之一）。
    **②问题与口径**：原生 tabBar 的图标尺寸**平台固定**（无 API 可调），能调的是**图片内容在画布中的占比**——原图 **114×114 画布**里内容占 **72%–93%**（`price-bar` 最宽 106×96 ⇒ 平台观感偏大）。⇒ 本轮**保持画布 114×114 不变**（不改平台缩放映射），把内容等比缩进 **72×72 框并居中**（长边恰 72 ⇒ `home` 62×72、`price` 72×65、`my` 72×72；占用率 ≤65%，线宽等比变细）。
    **③实现**：用 PIL 对**同一画布内**重排（LANCZOS 缩放 + 居中贴回；`-dim` 变体同法处理，保留其原有压暗口径）；原图 md5 留痕于本次提交说明与 KB（可逐张比对回滚）。
    **④守卫（防回归）**：新增 `tests/unit/t55-douyin-tabbar-icons.spec.ts`（**4 例**，**纯 Node**：`zlib` 解 PNG + 逐行反滤波求 alpha 包围盒，零新依赖、可进 CI）——画布仍 114×114／内容 ≤72×72 且占用率 ≤65%／三个选中态图标统一装入 72×72 框且居中（留白差 ≤2px）／`pages.json` 仍按「dim＝未选中、原图＝选中」引用且微信自绘栏仍用 `iconpark/`（不引用 PNG）。**变异实测**：把一张图标换回原图 ⇒ **3 例变红**，复原即绿。
    **⑤微信侧零影响（双向取证）**：微信产物 `app.json` 的 `tabBar.custom = true` ⇒ 原生 tabBar（含 `iconPath`）**不渲染**，底栏由 `custom-tab-bar` 以 `iconpark/*.svg` 38rpx 绘制 ⇒ **视觉与代码均未改动**；6 张 PNG 仅作为静态资源随包（未使用）。
    **⑥验证与发布**：`vitest` **484 passed／3 skipped**（含 t55 四例）、`vue-tsc` 0 错；抖音产物复核＝6 张图标内容 62–72px／`custom` 缺省／12 页；**P4-13 产物校验 `ok=true`**、包体积 **1.34MB**；`tma preview` 出码（短链 `https://t.zijieimg.com/iXxFD23R/`）＋ **`tma upload -v 0.0.2` 成功**（`🎉 Upload success`）。
    **⑦待真机**：抖音扫码看底 tab 三图标大小/居中/选中态是否与微信端观感接近（`device-acceptance-checklist.md` §8.3.7）；两图标 `-dim` 压暗态是否仍清晰。

32. **AI 试衣详情旧 loading → wot loading 门面；弹窗统一 wot Popup 门面；弹窗文案 HarmonyOS Sans（2026-09-22 主人三项）**
    **①主人三项**：「从 AI试衣列表点进详情，一瞬间跳出 loading（好像是旧的）⇒ 换成 wot UI 的组件 loading」；「我的 → 个人信息卡片 → 完善个人资料弹窗内有些文字用默认字体而非 HarmonyOS Sans，需修复」；「检查新项目里 Popup 弹窗，使用 Wot UI 组件中的 Popup（可看 skill／CLI）」。
    **②旧 loading 定位与处置**：`pages/aiTryOnResult/index.vue` 首查分支渲染 **自绘旧 spinner**（`.loading-spinner`＝80rpx 白边圆圈＋`border-top-color` 旋转），「生成中」面板内同一套 ⇒ 两处**全删**（产物残留 0），改走**门面 `ui/BaseLoading`**（内含 wot `wd-loading`；业务页零 `wd-*` 直用）——首查「加载中」竖排、处理中「生成中」竖排。
    **③字体真相与处置**：本仓 HarmonyOS Sans 是 `uni.loadFontFace` 注册的自定义字体＋**全局类 `.font-harmony`**（`App.vue:6-22/59`），**默认字体仍是系统字体** ⇒ 弹窗内只有挂类的节点才用 HarmonyOS；`ProfilePopup` 原本仅标题挂 `font-noto-serif` ⇒ **容器挂 `.font-harmony`**（全部文案继承；标题衬线优先）；同族 `LoginPopup` 同法修复。
    **④弹窗统一 wot Popup**：`ProfilePopup`／`LoginPopup` 由自绘 `.profile-overlay`／`.login-overlay` 迁到**门面 `ui/BasePopup`**（内含 wot `wd-popup`；抖音/无 wot 环境保留自绘分支 `base-popup-native`），**遮罩点击关闭语义不丢**（门面 `cancel` → 原 `emit('skip')`／`emit('close')`），并删除死样式。**审计结论**：仓内其余 `*-mask`／`*-overlay` 均为**图片渐变遮罩/状态角标**（`album-mask`/`hero-mask`/`photo-card-mask`/`status-overlay`）**不是弹窗**，无需迁移；可选后续＝`AppPhotoPicker` 的 busy 遮罩换 `BaseLoading`（非弹窗，属美化）。
    **⑤守卫**：新增 `tests/unit/t57-popup-wot-harmony.spec.ts`（门面内部确为 `wd-popup` 且保留自绘分支／两弹窗经门面渲染＋容器挂 `font-harmony`＋自绘遮罩类与样式**不得回流**／`ProfilePopup` 标题衬线优先／`cancel` 转发语义）；`t38` 断言同步（登录弹窗容器 `.login-card`）。
    **⑥验证**：`vitest` **497 passed／3 skipped**、`vue-tsc` 0 错、**三平台构建 exit 0**（h5／mp-toutiao／mp-weixin）；产物实证＝`pages/aiTryOnResult/index.js` 含 `base-loading`、旧 `loading-spinner` 残留 **0**、`ProfilePopup.json` 的 `usingComponents＝{base-popup: ../../ui/BasePopup}`、`ProfilePopup.wxml` 含 `font-harmony`。**待真机**：详情页 loading 观感（wot 竖排 loading）＋两弹窗文字字体。

    **⑦独立 CR 复核与处置（2026-09-22；子代理只读，含 `npx wot info`／基座样式表／产物实证；本批 CR 我起初漏做，事后退补）**：
    - **🔴CR1「`<input>` 字体根本没修到」**——根因不是「没继承 page」（`App.vue:135-138` 的 `page{font-family:'HarmonyOS-Sans-SC'}` 本就全局生效，产物 `app.wxss` 实证），而是⒜新弹窗被 **`<root-portal>` 摘出 page 子树**（故本次容器类承重、方向正确）⒝微信基座对 `wx-input`/`wx-input input`/`wx-input div` **硬编码 `font-family:UICTFontTextStyleBody`** ⇒ 容器字体到不了输入框。**已按仓内先例（`pages/demoDetail:474-476`、`pages/favorites:364-365`）给 `.app-input-field` 显式声明 `font-family:'HarmonyOS-Sans-SC'`**；产物两端（wxss／ttss）均实证编译到位。
    - **🔴CR2「迁移引入 wot 弹层白底直角」**——`.wd-popup{background:var(--wot-popup-bg,…white)}` 且本仓未定义任何 `--wot-*`（产物 grep 空）⇒ 620rpx 直角白盒包住 48rpx 圆角深色卡片。**已照 `BaseLoadingPopup` 家法补 `POPUP_TRANSPARENT_STYLE`（`--wot-popup-bg: transparent; --wot-popup-radius: 0;`）并以 `custom-style` 透传**（产物实证）。
    - **🟡CR3 `placeholder-class` 在本仓永不命中**（基座自造 class 不带 `data-v`，scoped 规则恒不匹配）⇒ 改**内联 `placeholder-style`**（含字体与颜色），死规则已删。
    - **🟡CR4**：门面显式化遮罩语义 `closeOnClickModal`（默认 true）并透传，不再依赖 wot 上游默认值；**🟡CR7** 删除被行内层级覆盖的静态 `z-index:1000` 死声明；**🟡CR9** 抖音自绘分支补 `@touchmove.stop.prevent` 防穿透；**🟡CR8** 「生成中」spinner 与文案间距回补 `margin-top:16rpx`；**🟡CR6** 孤儿 `@keyframes spin`／`overlayFadeIn` 全清（**清理时曾把 `@keyframes` 首行删净、残块导致大括号失衡、两端构建失败，已定位并修好**）。
    - **测试升级（🟡CR10）**：`t57` 由**纯字符串断言**升级为**含渲染级断言**——mount 两弹窗＋内联 `wd-popup` 桩（镜像「遮罩与内容是兄弟」结构）断 `zIndex=1001`、`customStyle` 含透明面、`closeOnClickModal=true`、**遮罩点击 `cancel`→`skip` 恰好一次**、**卡片内点击不关闭**；`t38` 注册 `wd-popup` 桩（消 `Failed to resolve component` 警报）。
    - **CR 结论采信与更正**：CR 指出本条 ②「默认字体仍是系统字体」口径不准（实为 root-portal 摘树）＋「旧 spinner 残留 0」仅对类选择器成立（keyframes 仍随包）——**两条均已在本段更正/清理**；CR 复核层级表（`BaseDialog 1100 > BasePopup 1001 > BaseLoadingPopup/PullRefreshIndicator/BasePicker 1000 > home-btn/custom-tab-bar 999 > CustomNavBar 998`）确认 1001 够覆盖。
    - **仍未闭环（待主人/真机）**：⒜`root-portal` 需**基础库 ≥ 2.25.2**（后台「最低基础库版本」核对）；⒝进场动画叠加（wot `is-deep` 使 scale 从 0.1 起 ＋ 卡片 `cardPopIn`）需真机比对后定去留；⒞`t24-mine` 把两弹窗整组件 mock 掉 ⇒ 「我的 → 卡片 → 资料弹窗」主链路无组件级覆盖；⒟可选后续＝`AppPhotoPicker` busy 遮罩换 `BaseLoading`、死组件 `LoadingBlock` 是否删（待拍板）；⒠真机三图（输入框与 placeholder 字体／弹窗四角是否露白／进场动画）。

33. **层叠家法：自定义 Tab 栏不可被页面内 z-index 覆盖（2026-09-23 主人报「弹层被 Tab 栏盖住」）**：`custom-tab-bar` 位于**独立元素树**（与 `root-portal` 同为运行时注入层），页面内 `z-index` 压不过它、`wx.hideTabBar()` 对它**不生效**（生态公认）。⇒ 家法：⒜**弹层显示期间隐藏 Tab 栏**——`custom-tab-bar` 暴露 `visible` 开关（根节点 `wx:if`）＋ `pageLifetimes.show` 兜底恢复，门面 `ui/BasePopup` 用 `getCurrentPages().getTabBar().setData({visible})` 协同（fail-soft）；⒝底部弹层**不用 `root-portal`**（回到页面内层叠）并显式 `z-index`（当前 2000）；⒞**层级梯队**：底部弹层 2000 > `BaseDialog` 1100 > `BaseLoadingPopup` 1001 > `BasePicker`/`PullRefreshIndicator` 1000 > 自绘标题栏 `CustomNavBar` 998 > 自绘 Tab 栏 900 > `BottomActionBar` 100 ⇒ 后来人**不要把 Tab 栏当最高层**。

34. **AI 推荐 / AI 试衣 Loading 页：进度文案加「环形进度动画」（wot `wd-circle` ＋ 绑定 Tokens；**增量**，三段式步骤条保留）（2026-09-23 主人目标）**
    **①主人原话**：「目前 AI 推荐与 AI 试衣的 Loading 页的进度文案没有动画，需要添加 wotUI 的动画与组件 https://wot-ui.cn/component/circle.html（用法可通过 CLI 和 Skill 查询），并绑定 Tokens」；追加口径：「**原有的三段式进度要保留**，给进度文案加上圆环进度动画（**做增量**）」。
    **②定位（增量前的现状）**：两处 Loading 面（`pages/aiRecommendLoading`、`pages/aiTryOnResult` 的等待面板）**共用** `components/GenerationProgress`（横向三段式步骤条 `gp-steps/gp-node/gp-line/gp-check` ＋ 静态 `<text class="gp-percent">{{ percent }}%</text>`）；伪进度由 `composables/use-fake-progress`（10s／28s 走满 99%，按秒跳变）⇒ **文案确实无动画**。
    **③落地（增量）**：⒜新增**门面** `src/ui/BaseCircle.vue`（包 wot `wd-circle`，业务组件零 `wd-*` 直用；事实源 `npx wot info Circle`：`v-model`／`size`(px)／`color`／`layer-color`／`speed`(rate/s)／`stroke-width`(px)／默认 slot 中心内容）；⒝**绑定 Tokens**：进度色 `tokens.semantic.colorAction`（品牌金）、轨道色 `tokens.semantic.colorBorder`（金 30%）、直径/描边新增 `component.progressCircleSizeRpx=200`／`progressCircleStrokeRpx=12`（rpx→px 经 `upx2px`，测试环境按 2:1 折算）；⒞`GenerationProgress` 仅把**百分比文案节点**替换为「`BaseCircle :model-value="percent" :speed="60"` ＋ 中心 slot `<text :key="percent">`（逐次重播 `gpPercentIn`）」，并把当前步骤文案挂 `gpLabelPulse` 脉动 ⇒ **三段式步骤条结构逐字保留**（`git diff` 仅百分比一处 + 样式）；⒟`.gp-percent` 色改 token `$color-action`；`margin-top` 上移到 `.gp-progress` 容器以保持与步骤条间距等价。
    **④验证（含变异）**：新增 `tests/unit/t59-ai-loading-progress-circle.spec.ts`（**6 例**：门面契约/token 单一事实源/**增量**语义（三段式仍在＋圆环内文案）/两处动效存在且 `.gp-label-active` 仅一条/两页共用组件/渲染级断言圆环收到 `percent` 与中心 `NN%`）＋ 新增 wot 桩 `tests/stubs/wot/wd-circle/wd-circle.vue`；**变异 3 组全红**（删 `:model-value` → 2 红；删 `:key` → 1 红；删脉动 → 1 红）；**全量 `vitest` 515 passed／3 skipped**、`vue-tsc` 0；**微信与抖音构建 exit 0**；产物实证 `GenerationProgress.json` 的 `usingComponents＝{base-circle: ../../ui/BaseCircle}`、`ui/BaseCircle.wxml` 含 `wd-circle`、`wxss` 含 `gpPercentIn`/`gpLabelPulse`。
    **⑦主人第二报「推荐 loading 进度会自动更新，但试衣 loading 不会」（2026-09-23）——根因＝computed 失去响应式依赖被缓存**：
    **取证**：内核 `application/ai-result-flow.ts` **本就每秒** `timers.setInterval(() => emit({ elapsedSeconds: state.elapsedSeconds + 1 }))`（:139-141）⇒ tick 源没问题；问题在 `use-fake-progress` 的 `progressPercent`：`effectiveElapsedSeconds()` 在 `startedAtMs` **有效**时**提前 return、不再读 `source.elapsedSeconds.value`** ⇒ 该 computed **没有任何响应式依赖** ⇒ Vue 缓存后**永不重算**。两页表现差异因此可解释：**试衣页**几乎总能取到服务端 `created_at`（⇒ 走墙钟分支 ⇒ 冻住）；**推荐页**新开一次 `resumed === 0` ⇒ 走回落分支（读 `elapsedSeconds`）⇒ 每秒刷新。**修法**＝在 `progressPercent` 内**无条件** `void source.elapsedSeconds.value;` 建立依赖（注释写明原因），墙钟/回落两条分支均随 tick 重算。**守卫**＝`t60` 新增「假定时器 + `vi.setSystemTime` 推进墙钟 + tick 递增 ⇒ 断言 25%→75%」用例：**去掉该行即红**（该用例本来就能抓住主人报的这条 bug）。**验证**：`vitest` **530 passed／3 skipped**、`vue-tsc` 0、微信构建 exit 0；体验版 **`v1.0.52`**（`TOTAL 1,711,361 B`，留档第 26 行）。

    **⑥CR 收口（第二轮，2026-09-23；主人报「等待过程中圆环不会自己推进」一并修）**：⒜**根因定位**：`wd-circle` 自带动画依赖**其内部定时器 + canvas 节点查询**（读源码实证：`reRender()` 用 `setTimeout` 链按 `STEP=1 / 1000/speed` ms 步进；`getContext()` 在 `#ifdef MP-WEIXIN` 分支**只有查到节点才 resolve**，查不到即永远 pending）⇒ 真机易「环停在初始值」。**修法**＝门面 `BaseCircle` **自补间**（每 `tickMs` 前进 `step` 个百分点，默认 `1/100ms`＝10%/s 与伪进度同速）+ `speed` 默认 **0**（wot 收到 0 直接绘制，不再走其内部定时器）；`GenerationProgress` **删除 `:speed="60"`**（二选一，防双跑）。⒝CR 🔴1：门面自补间原用 `setInterval`，被 `t47` 「收集 globalThis.setInterval 桩后取最后一个」的用例误抓 ⇒ 该用例转红；改**自调度 `setTimeout`**（与页面计时器解耦，到达目标即停）。⒞CR 🟡1：`upx2px` 原走 `globalThis.uni`，而 **mp-weixin 产物里不存在该全局**（vendor.js 仅 `globalThis.wx=e`；裸 `uni.*` 被编译为 `common_vendor.index.*`）⇒ 环恒 100px、token 的 rpx 语义不落地；改**注入桩优先 + 裸 `uni` 兜底**。**同根因连带真 bug**：`platform/uni/album-save.ts` 的 `uniApi()` 亦只查 `globalThis.uni` ⇒ 真机「保存到相册」**fail-closed 恒失败**（测试因桩了 globalThis 而测不出）⇒ 已改为回落裸 `uni`。⒟CR 🟡5：失败终态**唯一出口** `finishAsFailed()`（停表+置失败+清起始时间），覆盖超时/网络异常/非 ok/支付流程共 8 处；`t60` 改**结构不变式**守卫（helper 之外不得出现裸 `status.value = "failed";`）——原计数式断言在「删一处调用」变异下仍绿（假绿），改后变异即红。⒠CR 🟡4 假绿点补测：M7（注入假 `uni.upx2px` ⇒ 断言 size=110/stroke=6.6）、M8（`.gp-progress` 的 `margin-top:20rpx` 守卫）、M9（`wd-circle` 桩忠实镜像 `v-if="!text"` + 断言门面不传 `text`）。⒡CR 🟡3 **如实口径**：环内文案入场动画实际兑现的是 **opacity**（`scale` 在 `<text>` 的**非替换行内框**上被浏览器忽略 ⇒ 已加 `display:inline-block` 使其生效），且 **`:key` 在 mp-weixin 产物里是惰性属性、不触发重挂载** ⇒ 「逐次重播」在微信端有限（**真实生效的动效**＝环推进 + 当前步骤文案持续脉动 `gpLabelPulse` + 入场 opacity）。⒢CR 🟡6 小项：`.gp-percent` 加 `line-height:1`（消除 strut 造成的 ~1.5px 光学偏移）、起始时间键作用域化 `lm.aiRecommend.startedAt.v1:<shopId>:<filename>` 并改走 `uniStorage` 端口、`BaseCircle` 注明只读（不 emit `update:modelValue`）。**验证**：`vitest` **529 passed／3 skipped**、`vue-tsc` 0、**微信+抖音构建 exit 0**；变异复验 M7/M8/N3 均红。**待主人/设计确认**：环 200rpx ⇒ 等待面板高度 118px→205px（+87px，窄屏纵向留白；可下调至 160rpx）；后端 `created_at` 时区语义（若实为 UTC 则按 +08:00 解析会早 8h ⇒ 被 30 分钟新鲜度窗口挡掉＝**安全降级**为旧行为）。

    **⑤CR 结论与本批发布（2026-09-23）**：独立 CR 临时结论 **无 🔴**（实证：三段式逐字保留／`margin-top` 迁移后布局等价（H5 实测圆心与文本中心均 640px）／token 闭环（`.gp-percent` 实编译 `#f1cd91`、`wd-circle` 实收 `rgb(241,205,145)`）／**抖音端根本不构建 AI 六页**（`#ifdef MP-WEIXIN` 实证）⇒ TTSS 变量问题不适用／`<canvas type="2d">` 自 2.9.0 起同层渲染 ⇒ 不遮标题栏、可被弹层正常遮盖、无 `scroll-view` 嵌原生限制、首帧未绘制时**首个百分比变化即自愈**）；**该结论下已发体验版 `v1.0.50`**（`TOTAL 1,709,667 B`）。CR 另给 **3 处低成本收尾（不阻塞）** 与 **1 处测试假绿**：⒜推荐页失败/异常路径缺终态清理（与 commit message 自称「三处」不符）；⒝无时区 `created_at` 按本机时区解析（跨时区放大已等待时间）；⒞起始时间键未走存储端口/未按 op 作用域；⒟`t60` 页面接线守卫为源码字符串断言（5 组变异全绿）⇒ 待抽纯函数做行为测试。

    **⑤同批修复（主人本轮追加需求）：等待页「返回/切页再回来进度从 0 重来」（体感丢进度）**——根因：伪进度只认**本页挂载后的本地 `elapsedSeconds`**（新页面实例＝从 0），而异步任务其实照跑。解法（不改内核、不改轮询节奏）：`use-fake-progress` 新增可选 **`startedAtMs`（任务真实起始时间）** ⇒ 有效时按**墙钟**推导百分比（回页自动续算），无效（非数字／≤0／**未来**＝时钟偏差）回落本地累加（旧行为零变化）。取值口径：⒜**AI 试衣**＝**服务端任务 `created_at`**（`aiTryOnResult.queryInitialTask` 首查即解析，跨设备一致，最佳实践；解析失败/时钟偏差保持 0）；⒝**AI 推荐**＝该接口「只调一次不可轮询重发」⇒ **本地持久化起始时间** `aiRecommend:startedAt`（进页读取、无可续则写下本次开始），并设**陈旧保护**（超出 180s UI 超时窗口即忽略，避免「永远 99%」假进度），失败/超时等**终态统一出口**（`finishAsFailed()`，见本条第⑥段）。测试：`tests/unit/t60-progress-resume.spec.ts`（**4 例**：−5s 起始 ⇒ ≈50% 而非 0／无起始或 NaN 或未来 ⇒ 回落旧行为／续算仍守 99% 封顶与 `progressDone→100`／两页接线源码守卫）；**全量 `vitest` 519 passed／3 skipped**、`vue-tsc` 0、微信构建 exit 0；产物实证两页 `index.js` 均含 `startedAtMs` 接线。

    **⑤待办（本批）**：独立 CR（子代理只读）**在飞**——目标规定的「单测 → **独立CR** → 上传体验版」顺序要求**CR 回执后**再发版；届时按 🔴／🟡 处置并出 `v1.0.50`。
