# 已知偏差登记（P0-09；行为修正必须写明产品依据与测试，不盲保缺陷）

1. **package/lock 历史差异**：以 preflight §0 实测哈希为准（lock 60a176ad…／pkg 9f522349…）。
2. **finalScore 类型缺失**：旧端 AI 推荐结果 `finalScore` 运行时有值、类型未声明 ⇒ 新端补齐类型（嘉仪 PR#1 已对齐后端降序口径）。
3. **下载 paid 与权益到账竞态**：旧端 payGuard.uts 统一支付门闩＋到账双条件 paid&&balance>0＋续跑前重拉余额＋不自动二次拉起支付 ⇒ 新端保语义。
4. **品牌馆开关刷新口径（plan §7 有意修正，2026-09-17 校正表述）**：新端**冷启动（首页 onMounted）**刷新开关，**同上下文在飞请求去重**（brand-hub-controller 按 scopeRevision 去重、切品牌作废在飞）；旧端 60s 内存缓存＋下拉/热恢复强制刷新**不再保留**（controller 无缓存，且当前**未实现** onPullDownRefresh——原表述「下拉/热恢复强制刷新」与代码不符，此处更正）。**2026-09-17 P2-20 追加**：品牌馆页切品牌后回首页，由**首页 onShow 检测品牌基线变化**触发 bumpScope＋复位入口开关＋重载（旧端 index:218-232 同序，非偏差），保证「入口显隐」与「品牌数据」同步，杜绝入口显示而进入被拦。
5. **vitest 版本适配（2026-09-17 实测）**：vitest 5.0.1 需 vite `./module-runner` 导出（vite 6+），与冻结的 vite 5.2.8 不兼容（启动即 ERR_PACKAGE_PATH_NOT_EXPORTED）⇒ **降级锁 vitest 3.2.4**（其 dependencies.vite＝^5.0.0），不为测试框架盲升 vite。
6. **工具链测试自身两处 bug（2026-09-17 首跑抓出并修复）**：①精确版判定正则写错（`[^~]` 语义相反）②`@dcloudio/types@^3.4.8` 属独立版本线被误判跨线 ⇒ 修正为显式首字符判定＋发行线检查限定 `3.0.0-` 族；**负向 fixture 先红后绿**，10/10 通过。
7. **品牌馆页解析口径（P2-20 CR 🟡4，已知低风险）**：gate 取件的严格性由 client `isBusinessSuccess`（0/200 均成功）承担，旧端 `pageConfig.uts:11` 为严格 `code===200` ⇒ 若接口返回 `code=0` 旧端隐藏、新端可能放行（实测返回 200，概率低）。`domain/brand-hub.parseBrandHubResponse` 保留为旧端口径的规格测试锚点，生产链路已不经它。
8. **webview URL 准入加固（P2-22，**有意修正**）**：旧端 `pages/webview/index.uvue:24-26` 对 `url` 参数**零校验**（`decodeURIComponent` 后直接透传 `<web-view :src>`）⇒ 可打开任意外部 URL（含 `javascript:`/`data:` 等）。**产品依据**：phases P2-22「webview 只允许既有合法 URL 策略…不放开任意 URL」。**新端口径**：仅 `http(s)` 且 host 精确命中或其子域（白名单＝Profile `apiBases` 派生域，兜底 `lanmei66.cloud`/`crazyma99.xyz`；沿用 `imageLoader.uts:33-38` 精确主域口径防子串伪装）；非法/缺失 → 不渲染 web-view＋toast「链接不可打开」＋800ms 返回。协议（policies/user、policies/privacy）与联系信息（ServiceContact 电话/二维码）走**站内页**，不经 webview；第三方 URL 一律拒绝。**测试**：`tests/unit/t29-webview.spec.ts`（6 例：白名单主域/子域/大小写/端口/userinfo 放行；外域/伪装子串/`javascript:`/`data:`/`file:`/相对路径/空值拒绝；页面拒绝路径＋返回按钮）。

9. **推荐空载荷判失败＝有意比旧端更严（2026-09-17，T9b CR 🟡3）**：旧端判据为 `(code===0||200) && res.data`（`aiRecommendLoading:146`），在 JS 真值语义下 `{}` 与 `{analysis:null,recommendations:[]}` **都算成功**（转场到**空白结果页**）；新端 `application/ai-recommend-flow.ts` 对「整包全空」判 `BUSINESS` 失败并给「推荐结果为空，请重试」。**无误伤**：真正合法的「有 `analysis`＋空 `recommendations`」仍 `ok:true`。**产品理由**：空白结果页对用户是死路，失败提示＋重试入口更可用；如需逐字等价请指示。
10. **finalScore 数字字符串口径（2026-09-17，T9b CR 🟡5，可选登记）**：旧端 `v-if="rec.finalScore > 0"` 在 `"90"`（字符串）时成立会显示「90分」；新端 `normalizeFinalScore` 仅接受 `number` ⇒ 字符串不显示。现行后端 DTO 为 number，**实务等价**。

11. **字体全局类未定义（2026-09-17 补登，CR 🔴2 触发）**：旧端 `App.uvue` 全局类 `font-noto-serif`／`harmony`（宋体/鸿蒙字族）在新端**未定义**，样式沿用既有口径：字号按旧值**字面量**还原（如旧 `--font-size-body=24rpx` ≠ 新 `tokens.semantic.fontSizeBody=32rpx`，不得混映射），字体族差异由后续主题批次统一处理。**产品依据**：字体族缺失属视觉细节，不阻断功能闭环；**测试**：各批页面单测按字面量断言字号（如 `t22`／`t26`／`t32`），本表 `platform-capability-matrix.md` 引用本条。此条为补齐「矩阵引用不存在的登记」的缺口而补登（此前该偏差**未登记**，违反「偏差必须进 deviations」纪律）。

12. **店铺封面追加 `lazy-load`（2026-09-17，性能偏差；排查「模拟器卡死」时引入）**：旧端 `components/PhotoGrid/PhotoGrid.uvue` 的店铺封面（`<image mode="aspectFill">`）**无懒加载**（`grep lazy-load` = 0），新端初版按忠实移植同样未加；但首页会**一次性渲染全部店铺封面**，在微信模拟器/低端机上首屏容易长卡。**改法**：仅给该 `<image>` 追加 `lazy-load`——**只改变加载时机**，不改布局类名、尺寸、圆角与点击行为。**产品依据**：性能与可感知卡顿；**测试**：`tests/components/ui-contract.spec.ts`、`t6-index.spec.ts` 与三平台构建均通过（无断言依赖图片加载时机）。

13. **骨架栅格用「包裹 view + class」替代通配选择器（2026-09-17，平台强制；首次体验版上传实测触发）**：`pages/index/index.vue` 与
    `pages/priceHomePage/index.vue` 的骨架占位原用 `.sk-grid > * { flex: 1 }` / `.sk-row > * { flex: 1 }`（**通配选择器 `*`**）。
    新版代码在 `uni build` 阶段不报错，但**微信 wxss 编译器拒绝 `*`**：体验版上传时编译失败
    `wxss 编译错误 ErrorFileCount[2] ... error at token \`*\``（`pages/index/index.wxss`、`pages/priceHomePage/index.wxss`）。
    **改法**：子元素各包一层 `<view class="sk-cell">`，CSS 改 `.sk-cell { flex: 1 }`——**视觉与布局等价**（`.sk-grid/.sk-row` 仍为
    `display:flex; gap:8rpx`，两个占位仍各占 1 份），仅多一层 view（骨架为临时占位，不影响业务 DOM）。
    **产品依据**：微信 wxss 不支持通配选择器（平台限制，非可选）；**测试**：全量 vitest 400 passed、`vue-tsc` 0 错、三平台构建 0，
    且**产物实测无 `*` 选择器**（`grep '*.data-v'` 与通配正则均 0 命中），体验版上传成功（`v1.0.25`）。

14. **「暗→亮」偏差撤回，深色主题还原（2026-09-19，主人拍板）**：T4 起 tokens semantic 层曾按亮色定义（`colorPage=#FFFFFF`、正文 #333/#666 等），各页注释称「全仓迁移主题口径」，但**主人从未拍板**；抖音端实测「页面背景应为黑色却是白色」后撤回。**还原口径**：`tokens/source.json` semantic 改回旧端深色实测值——`colorPage→primitive.ink #160F04`、`colorTextPrimary/Strong→gold #F1CD91`（旧端正文/标题主色，`demoDetail.uvue:997/:1132`、`index.uvue:758`）、`colorTextSecondary→rgba(241,205,145,0.7)`、`colorTextMuted→rgba(241,205,145,0.5)`、`colorBorder/Divider→rgba(241,205,145,0.3)`；`pages.json` globalStyle 底色/导航栏同步深色（对齐旧端 `src/pages.json:67-72`）。为此 `generate-tokens.mjs` 字面值校验放开 `rgb()/rgba()`（标准 CSS 颜色函数，wxss/ttss 均支持），并补 primitive 层进 theme.scss/theme.css。页面级亮色适配残留（黑文字/黑底骨架在深底不可见）已按旧端 uvue 逐值还原（favorites/mine/brandHub/aiTryOnHistory/demoDetail，详见各文件 2026-09-19 注释）。policies 两页旧端本就是白底黑字，保持不动。**已知遗留**：`mine` 页 `.profile-group` 金色系描边为接入期自主偏差（`84a24f0`），非本次亮色残留，未动。

15. **抖音 TTSS 不支持 CSS 变量 ⇒ 全仓消除运行时 CSS 变量（2026-09-19，平台强制）**：抖音官方 TTSS 文档「变量等特性编译暂不支持，依赖宿主内核兼容性」。Vue3 SFC `v-bind("tokens…")` 编译产物为 `var(--hash)`＋内联注入（`useCssVars`），抖音端整条丢弃 ⇒ 体验版「分类 tab 样式全丢/页面白底」根因。**改法**：①184 处 `v-bind("tokens.*")` → scss 编译期 `$变量`（`scripts/codemod-vbind-scss.mjs`，style 块补 `lang="scss"`；`vite.config.ts` 以 `additionalData` 注入 `src/generated/theme.scss` 原文，token 单源不变）；②直写 `var(--*)` → 字面量（`scripts/codemod-css-var-literal.mjs`，映射表取自 App.vue `page{--*}` 定义块/design-token.md，注释跳过不篡改）；`App.vue` 的 `page{--*}` 定义块随之删除；③`GenerationProgress` 局部 `--gp-node-size` 改 scss 局部变量。**验证**：mp-toutiao 产物 `grep var(--`/`useCssVars` 清零（唯一残留为 wot-ui 探针页引用的 uni 框架变量 `--window-bottom`，非生产路径）。**产品依据**：平台限制，非可选；微信/H5 视觉不变（值为编译期同一常量）。

16. **抖音端自定义导航栏对齐微信（2026-09-19，主人拍板）**：原口径「抖音 navStyle=default、CustomNavBar 渲染空、系统栏接管」导致抖音端所有页面无自绘导航，且 slot 内容（客片列表/收藏的搜索框）一并消失。现撤回：pages.json 九个业务页 `navigationStyle: custom` 去除 `#ifdef MP-WEIXIN` 全平台生效（AI 六页本就仅微信注册，不受影响），CustomNavBar 删除 `nativeHidden` 平台门控全平台渲染；同步补回 demoDetail/favorites 搜索框 `placeholder-style`（金色 70% 字面量，不用 var()）。**测试**：`t6-components.spec.ts` 抖音用例反转为「与微信同口径渲染」。**风险**：抖音右上胶囊（含「反馈」位）比微信宽，搜索栏等 slot 内容在窄屏可能与其视觉相碰，待真机目视。

17. **抖音导航口径二次回退＋全端页脚距底 20px（2026-09-19 晚，主人实测反馈）**：#16 的「抖音 navigationStyle custom 全平台生效」实测**未生效**——抖音平台 custom 导航需在开放平台「功能管理-页面结构自定义」**申请权限**（未申请则平台仍绘制默认栏：logo＋页面标题＋胶囊，且配置 custom 会阻碍代码包上传），叠加 CustomNavBar 全平台自绘 ⇒ 我的/价目表等页**双标题＋双顶部占位**（IDE 模拟器截屏实证）。**回退口径**：①`pages.json` 九个业务页恢复 `#ifdef MP-WEIXIN` 包裹 `navigationStyle: custom`（抖音回到 default 系统栏，单标题）；②CustomNavBar 抖音端仅**内联渲染 slot**（favorites 搜索框/targetPhotoDetail 标题组——#16 要救的 slot 内容保留），自绘标题栏＋占位仅微信渲染；demoDetail 搜索框本批已下移页面首行，不受影响。③页脚：主人指示「底部文案距底给 20px」——PageFooter 四变体下边距 32rpx→40rpx（=20px），抖音端文案不再贴原生底 tab，微信 tab 页/子页同步抬高。**测试**：`t6-components.spec.ts` 抖音用例再反转为「不自绘标题栏、仅透传 slot」。**后续可选**：若主人在抖音开放平台申请「页面结构自定义」权限通过，可重新评估 #16 方案（届时仍需处理抖音 custom 下系统 logo/胶囊不可隐藏）。

18. **demoDetail 搜索框回导航栏 slot＋AI 试衣按钮/详情底部补齐（2026-09-19，主人实测反馈「渲染混乱」逐条对齐原版微信）**：①demoDetail 搜索框从页面首行**回到 CustomNavBar slot**（旧端 :4-19/:929-958 逐值还原，与 favorites 同款：描边胶囊壳＋内置搜索 icon、无独立搜索按钮、键盘 confirm 触发、manual-back 有值先清空）；在 #17 口径下该 slot 微信＝自绘栏内、抖音＝系统栏下内联首行，两端同源（#17 注「demoDetail 搜索框下移页面首行」由此条取代）。②demoDetail 列表卡**补回 AI 试衣按钮**（旧端 :164-167/:1091-1110：desc-main 右侧 140rpx 占位＋desc-tryon 绝对定位垂直居中，`/static/aitry-btn.png` 随之拷贝进新端 static）；AI 页仅微信注册 ⇒ 按钮 `platform==='mp-weixin'` 门控（跳转函数同口径守卫）。③targetPhotoDetail **补回 BottomActionBar**（旧端 :77-90：AI 试衣按钮组 slot——微信门控；内置版权 footer 全平台——即主人所见「底部描述文案」；页脚文案经 page-config 用例注入，口径同 demoDetail/favorites）＋200rpx 底部占位（旧 :605-608）；`goToAiTryOn` 五参数口径（albumId/shopId/category/subCategory/style，hapticTap＋style 重编码）。**测试**：全量 vitest 414 passed；mp-weixin/mp-toutiao 双构建产物实证——微信 demoDetail.json 含 `navigationStyle:custom`、抖音无（#17①生效）；两端 wxml/ttml 均见 search-bar-wrap/desc-tryon/ai-tryon-btn，BottomActionBar 组件产物含 bottom-bar/bar-copyright，`aitry-btn.png` 入两端 static 并经 common/assets.js 绑定引用。

19. **搜索框盛满导航栏剩余区域＋AI 推荐横幅几何还原（2026-09-19，主人实测「搜索框偏小/AI 模块底部截断」）**：①demoDetail/favorites 导航 slot 搜索框 `width:65%`→`flex:1`（margin 0 20rpx）——旧端 65% 建立在旧 CustomNavBar slot **无胶囊避让**的满宽上（≈430rpx）；新端 slot 已被胶囊避让 paddingRight≈95px 收窄，照抄 65% 只剩 ≈306rpx。slot 避让层保证填满后不撞胶囊。②demoDetail AI 推荐横幅按旧端 :1191-1226 还原：banner 定高 **178rpx**（初版背景图在流撑 160rpx＋内容 absolute 超高 ⇒ 被 overflow 裁底）、`margin:24rpx 20rpx`、背景图绝对铺满、内容相对定位 height:100% **双向居中**（初版 absolute inset:0＋左对齐 padding 系偏差）、标题 38rpx（--font-size-display，初版误取 sub-title 档）/副标题 22rpx·margin-top 16rpx·letter-spacing 1rpx（初版 8rpx/caption 档）。**测试**：全量 vitest 417 绿（t24 抖音风味文案失败系后台连跑 mp-toutiao 构建后 `src/generated/profile.config.ts` 残留抖音 profile 所致——`gen-profile-local.mjs mp-weixin` 回切微信快照即绿，非本次改动回归）；双端构建产物实证 `.search-bar-wrap{flex:1}` 与 banner 178rpx 写入 wxss/ttss。

20. **PhotoGrid 容器补 `box-sizing:border-box`（2026-09-19，抖音价目表「店铺列表贴紧右侧」实测）**：`.demoPhotoContaner` 为 `width:100%`＋`padding:0 8rpx`——旧端 uvue 默认 border-box 不溢出；vue3 mp 端默认 content-box ⇒ 右溢 16rpx，右列店铺卡贴紧/溢出右屏边（微信同机制，8rpx≈4px 不易察觉）。卡片级 border-box 此前已补（本组件 :80 注释），容器级本次补齐。**页脚 20px 口径核查**：PageFooter 四变体下边距 40rpx（=20px）经 #17 已全部生效，本次双端产物复核 wxss/ttss 逐变体在案（beian/bottomdesc/copyright/bottomdesc-dark 及 BottomActionBar 的 28rpx+12rpx=40rpx）；mine 页新旧端均无版权 footer，不涉及。**测试**：全量 vitest 绿＋双端构建产物实证 `.demoPhotoContaner` 含 `box-sizing:border-box`。

21. **PageFooter 分隔线以文案为闸（2026-09-19，主人实测「客片列表 loading 下方有一条横线」）**：loading 期页脚无条件渲染，`.divide`（2rpx 15% 白线）先于文案出现，双端可见。**口径**：底部文案不展示之前不出横线——divide 改为 `withDivide && (mainLine/supportLine 任一行非空)` 才渲染（loadFooter 兜底链 override→OPS→Profile→默认保证就绪后非空）。组件级收口，9 页同源生效，无需逐页改。**测试**：`page-footer.spec.ts` 增闸门用例（空串不渲染→文案到达后出现），copyright/wrapped 两例补文案 props 维持原断言；全量 vitest 绿＋双端产物实证 divide 为 `wx:if/tt:if` 条件节点。

22. **客片详情「服务价格/套餐内容」两行恢复无条件渲染（2026-09-19，主人实测「抖音客片详情没展示套餐内容」）**：初版迁移给两行加了 `v-if` 空值守卫（`price != null`、`packageDesc != null && !== ''`）——**未登记的自主偏差**；旧端 :46-54 无条件渲染，空值时仍显示「套餐内容：」标签行。实测 trial 接口（`GET /wechat/album/detail`，平台无关、无 platform 头）对空套餐相册返回 `packageDesc:""`（如相册 463），新端整行消失 ⇒ 主人所见「没展示」。**还原**：两行去掉守卫与旧端一致（空值渲染空串，标签保留）。**测试**：全量 vitest 415 passed＋双端构建产物实证 `.package` 行无 `wx:if/tt:if` 守卫。

23. **PageFooter beian 下间距 margin→padding（2026-09-19，主人实测「抖音首页/价目表底部描述仍贴底 tab」）**：#17/#20 已把 `.beian` 下边距抬至 40rpx 且双端产物在案，但抖音端**页面滚动高度不计最后一个元素的 margin-bottom**（小程序通坑，margin 在 CSS 里但不产生视觉间距）⇒ 40rpx 形同虚设。**改法**：`.beian` 下间距 `margin: 40rpx auto` → `margin: 40rpx auto 0; padding-bottom: 40rpx`（padding 计入盒高、必计入页面高度）；仅 beian 变体（index/priceHomePage 两 tab 页，主人点名范围），bottomdesc/copyright/bottomdesc-dark 为子页页脚无底 tab，未动。微信端视觉不变（`.beian` 无背景，padding 与 margin 等价；其后 tabbar-safe-spacer 衔接不变）。**测试**：`page-footer.spec.ts` 7 绿＋全量 vitest 415 passed＋抖音产物 ttss 实证 `.beian{margin:40rpx auto 0;padding-bottom:40rpx}`。
