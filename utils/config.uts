// 接口环境配置
export interface HttpConfig {
  baseURL: string
  timeout: number
}

export function getHttpConfig(): HttpConfig {
  // TODO: 替换为你的域名
  const baseURL = 'https://crazyma99.xyz:8155'
  return { baseURL, timeout: 15000 }
}
